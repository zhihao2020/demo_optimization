\documentclass[a4paper,fleqn]{cas-sc}

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{placeins}
\usepackage{multirow}
\usepackage{siunitx}
\usepackage{array}
\usepackage{tabularx}
\usepackage{mdframed}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc,backgrounds,decorations.pathreplacing}
\graphicspath{{figures/}}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{0pt}
\raggedbottom

\begin{document}
\let\WriteBookmarks\relax
\def\floatpagepagefraction{1}
\def\textpagefraction{.001}

\shorttitle{Feasible-support hybrid TD3 for nonconvex CAES actions}
\title[mode=title]{Feasible-Support Hybrid TD3 for Multi-Energy Scheduling with Nonconvex Compressed Air Energy Storage Actions}

\begin{abstract}
Weekly price-taker dispatch of a thermal--battery--compressed-air (CAES) plant must settle Shandong \emph{monthly} agency-purchase time-of-use (TOU) cash together with a China National ETS carbon charge, spill and unserved penalties, and battery wear. The plant is a first-principles differential--algebraic (DAE) object: multi-tank adiabatic CAES with thermo-gas coupling, rather than an algebraic energy-balance surrogate. The CAES electric command occupies a disconnected device envelope that shrinks further with inventory margins, and interchange capacity couples thermal, battery and CAES commands, so the admissible hybrid action is the \emph{joint} support \(\mathcal{A}_f(s)=(\mathcal{A}_T\times\mathcal{A}_B\times\mathcal{A}_C)\cap\mathcal{A}_{\mathrm{grid}}\), not a cartesian product of device boxes. A continuous policy on a single box followed by projection is a poor match to that topology. This paper states the plant on a MWORKS Sysplorer Modelica model exported as an FMI functional mock-up unit (FMU)---FMI is the exchange standard, not a claimed novelty---and proposes \emph{physics-constrained parameterized hybrid TD3} (PC-HybridTD3). The actor emits \((u^{\mathrm{th}},u^{\mathrm{bat}},m,z)\) through an analytic joint decoder onto \(\mathcal{A}_f(s)\); the critic scores the same hybrid packing; only twin-executed hours enter the economic Bellman update. GiveSafe is a last residual screen: greedy evaluation tries the actor once. Projected continuous TD3 and a static-band hybrid TD3 are the two ablations. The numerical study is a fair weekly protocol under the 2026 monthly 110~kV path. The claim is support-consistent hybrid learning on the DAE twin, not hierarchical RL and not unconditional cash dominance over MILP.
\end{abstract}

\begin{highlights}
\item Parameterized hybrid CAES action $(m,z)$ on disconnected charge / idle / discharge regions, avoiding the inactive gap of box projection.
\item Joint feasible support $\mathcal{A}_f(s)=(\mathcal{A}_T\times\mathcal{A}_B\times\mathcal{A}_C)\cap\mathcal{A}_{\mathrm{grid}}$: analytic CAES then thermal then battery decoder; GiveSafe is a last screen, greedy tries once.
\item FMU-in-the-loop PC-HybridTD3: physical-only economic replay, 36/8/8 weekly split, rule / rolling MILP / projection TD3, noisy-forecast and 8760~h deployment checks.
\end{highlights}

\begin{keywords}
Multi-energy system \sep hybrid-action reinforcement learning \sep parameterized hybrid TD3 \sep feasible action support \sep comprehensive cost \sep carbon pricing \sep time-of-use tariff \sep Modelica \sep FMU \sep CAES
\end{keywords}

\maketitle

% =====================================================================
% Nomenclature (framed two-column symbol table)
% =====================================================================
\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.1}
\begin{mdframed}[linewidth=0.7pt,linecolor=black,
  innerleftmargin=6pt,innerrightmargin=6pt,
  innertopmargin=4pt,innerbottommargin=4pt]
  \centering\textbf{Nomenclature}\par\medskip
  \begin{tabularx}{\linewidth}{@{}l X @{\quad} l X @{}}
    \toprule
    \multicolumn{4}{@{}l}{\textit{Indices and sets}} \\
    \midrule
    $t$ & hour index in a weekly episode, $t=0,\ldots,T-1$ ($T=168$) &
    $i$ & device / inventory index \\
    $\mathcal{S},\mathcal{A}(s)$ & state space and state-dependent hybrid action support &
    $\mathcal{K}(s),\mathcal{M}_k(s)$ & admissible CAES modes and magnitude interval for mode $k$ \\
    $\mathcal{F}(s)$ & dynamic feasible set used to build $\mathcal{A}(s)$ &
    $k_t,\,z_t$ & CAES mode and normalised magnitude ($\mu_t\equiv z_t$) \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{Plant and market variables}} \\
    \midrule
    $P_t^{\mathrm{th/bat/caes}}$ & thermal / battery / CAES electric power &
    $P_t^{\mathrm{w/pv}},P_t^{\mathrm{load/grid}}$ & wind / PV / load / grid power \\
    $P_t^{\mathrm{curt}},P_t^{\mathrm{uns}}$ & curtailment and unserved power &
    $G_t,v_t,T_t^{\mathrm{air}}$ & irradiance, wind speed, ambient temperature \\
    $s_{t,\mathrm{bat/gas}}$ & battery and CAES gas SoC &
    $s_{t,\mathrm{hot/cold}}$ & CAES hot/cold thermal inventories \\
    $p_t,T_t^{\mathrm{gas}}$ & CAES gas pressure and temperature &
    $\eta,E_{\mathrm{cap}},p_{\mathrm{norm}}$ & battery efficiency, energy capacity, gas rating \\
    $\lambda_t^{\mathrm{buy/sell}}$ & TOU purchase / sale prices &
    $E_t^{\mathrm{buy/sell}}$ & grid purchase / sale energy \\
    $\Delta J_t^{\mathrm{cash}},J^{\mathrm{cash}}$ & cash-flow increment and weekly cash objective &
    $\Delta m_t,\pi_{\mathrm{CO_2}},J^{\mathrm{gen}}$ & CO$_2$ mass, ETS price, comprehensive objective \\
    $C_t^{CUT},C_t^{\mathrm{deg}}$ & curtailment/unserved cost, battery cycle cost &
    $C_t^{\mathrm{su}},C_t^{\mathrm{grid}}$ & CAES startup cost, soft interchange-contract cost \\
    $C_t^{\mathrm{op}}$ & device O\&M / fuel cash cost &
    $CC$ & comprehensive cost $CC=-J^{\mathrm{gen}}$ (primary ranking) \\
    $u_{\mathrm{tp}},u_{\mathrm{bat}},u_{\mathrm{caes}}$ & thermal / battery / CAES dispatch commands &
    $T,\,o_t$ & episode length (168~h) and 166-dimensional observation \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{Physics-constrained hybrid TD3}} \\
    \midrule
    $s_t,\,a_t$ & MDP state and physical FMU action &
    $e_m\in\{0,1\}^3$ & one-hot CAES mode (discharge / idle / charge) \\
    $M(s)$ & Boolean mode mask from $\mathcal{K}(s)$ &
    $\pi_{\theta}$ & parameterized actor $(u^{\mathrm{th}},u^{\mathrm{bat}},m,z)$ on $\mathcal{A}_f(s)$ \\
    $Q_{\phi_{1,2}}$ & twin critics on $(u^{\mathrm{th}},u^{\mathrm{bat}},e_m,z)$ &
    $\tau_{\mathrm{G}}$ & Gumbel--Softmax temperature ($1.0\to 0.2$) \\
    $r^{\mathrm{ext}}$ & extrinsic plant reward from $J^{\mathrm{gen}}$ &
    $\Pi_{\mathcal{F}}$ & GiveSafe acceptance onto $\mathcal{F}(s)$ \\
    $\gamma$ & discount factor &
    $\mathcal{D}_{\mathrm{B}},\mathcal{D}_{\mathrm{S}}$ & economic replay / safety audit set \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{Abbreviations}} \\
    \midrule
    PC-HybridTD3 & physics-constrained parameterized hybrid TD3 &
    TD3 & twin delayed DDPG (main solver; projection is the continuous ablation) \\
    MILP & mixed-integer linear programme (rolling surrogate) &
    TOU & time-of-use \\
    CAES & compressed-air energy storage &
    FMU & functional mock-up unit (FMI) \\
    ETS & emissions trading scheme &
    GHTD3 & goal-conditioned hierarchical TD3 (related; not the method) \\
    \bottomrule
  \end{tabularx}
\end{mdframed}
\end{table}
\FloatBarrier

% =====================================================================
\section{Introduction}
\label{sec:introduction}
% =====================================================================

Deep decarbonisation and China's national carbon market couple electricity tariffs with abatement costs at multi-energy plants~\cite{zhou2023iesdrl,yang2024planning,icap2025chinaets}. Shandong industrial and commercial users settle a \emph{monthly} agency-purchase energy price under official seasonal time-of-use (TOU) windows and float ratios~\cite{ndrc2021tou,shandong2023tou}. An emissions trading scheme (ETS) attaches a monetary charge to thermal output and grid imports. This paper studies \emph{plant-level price-taker operation} of a thermal--battery--CAES facility~\cite{emmanuel2020market,pei2024pricetaker}. The weekly objective is net cash flow after ETS-priced carbon, curtailment and unserved penalties, battery cycle cost, CAES startup cost and a soft interchange-contract charge. Week-end inventory enters only as a soft terminal bonus. Hybrid continuous--discrete commands, CAES mode locks and thermal ramps make unconstrained interaction with the plant both operationally unsafe and computationally awkward.

Model-based programmes remain useful baselines, but they typically convexify CAES thermodynamics or face tight budgets on hybrid spaces~\cite{cui2025ghtd3}. Deep reinforcement learning (DRL)~\cite{sutton2018rl,fujimoto2018td3,haarnoja2018sac} learns closed-loop policies by interaction. In multi-energy DRL the training environment is often an algebraic energy balance or a fitted map, including off-design quadratic $\dot m(P)$ CAES surrogates~\cite{zhou2023iesdrl,wu2024pricing}. Closed-loop evaluation on an FMI-exported multi-physics Modelica model of multi-tank CAES thermo-gas DAEs is less common. That object, rather than the FMI file format, is why the legal command is a state-dependent hybrid support instead of a box. Parameterized-action actor--critics already exist in the RL literature~\cite{fan2019hybrid,haarnoja2018sac}. Cui and co-authors have shown that storage in multi-energy dispatch has a genuine cross-timescale coordination problem; GHTD3 realises long-horizon inventory planning through a high-level SMDP, a low-level goal-conditioned MDP and historical relabeling~\cite{cui2025ghtd3,cui2024caesbess}. Multi-agent hierarchical energy managers follow a related path~\cite{jendoubi2023hier,ochoa2024bidding}. However, CAES introduces an \emph{orthogonal} challenge that is not captured by a standard continuous control space: its compression, idle and expansion regimes form a disconnected hybrid action domain, while the admissible magnitude of each mode further varies with thermodynamic states. This work therefore focuses on feasible hybrid action learning rather than introducing an additional temporal hierarchy. Option initiation sets can restrict when a mode may start; they do not, by themselves, put a same-hour policy on $\mathcal{K}(s)$ and $\mathcal{M}_k(s)$.

On the Sysplorer twin used here the CAES command is disconnected and further restricted by state. The policy is therefore defined on
\begin{equation*}
\mathcal{A}_f(s)
=
\mathcal{A}_{\mathrm{tp}}(s)\times
\mathcal{A}_{\mathrm{bat}}(s)\times
\bigcup_{k\in\mathcal{K}(s)}
\bigl(\{k\}\times\mathcal{M}_k(s)\bigr),
\end{equation*}
where $\mathcal{K}(s)$ is the set of currently admissible modes and $\mathcal{M}_k(s)$ the allowable magnitude interval for mode~$k$~\cite{garcia2015safe,sun2024constrained}. We develop \emph{physics-constrained parameterized hybrid TD3} (PC-HybridTD3): a same-timescale deterministic actor that emits $(u^{\mathrm{th}},u^{\mathrm{bat}},m,z)$ already decoded onto $\mathcal{A}_f(s)$, a twin critic on the same hybrid packing, and an economic Bellman update that uses only FMU-executed transitions. GiveSafe is the final execution screen; we do not present it as a new shield. The distinction from post-hoc projection is
\[
a_t\sim\pi_\theta(s_t)\;\Rightarrow\; a_t\in\mathcal{A}_f(s_t),
\qquad\text{not}\qquad
a_t=\Pi_{\mathcal{A}_f}\bigl(\pi_\theta(s_t)\bigr).
\]

The three gaps form a causal chain rather than three independent firsts. (1)~\textbf{DAE object.} Weekly thermal--battery--CAES operation under monthly Shandong agency-purchase TOU and National ETS, with device envelope $u_{\mathrm{caes}}\in[-1,-0.33]\cup\{0\}\cup[0.86,1]$, mode locks and inventory margins, stepped on an FMI-exported Sysplorer twin. Algebraic environments hide the support that the twin actually accepts. (2)~\textbf{State-dependent hybrid support.} The scheduling object needs $\mathcal{A}_f(s)$ written into both actor and critic, beyond a box continuous action or an inventory-goal hierarchy. (3)~\textbf{Executable same-timescale solver.} With three modes decided jointly with magnitudes every hour, a parameterized hybrid TD3 that never Bellman-updates a twin-rejected self-loop is a direct design. Hierarchical RL can still address the same plant; we do not argue otherwise.

Table~\ref{tab:lit} positions related work.

\begin{table}[!htbp]
\centering
\caption{Comparison of related scheduling studies (selected).}
\label{tab:lit}
\scriptsize
\begin{tabular}{@{}p{2.15cm}cccccp{1.55cm}@{}}
\toprule
Study & Hybrid act. & Safety & High-fid.\ twin & State supp. & Market & Method \\
\midrule
Zhou et al.~\cite{zhou2023iesdrl} & -- & -- & -- & -- & tariff / cost & flat DRL \\
Sun et al.~\cite{sun2024constrained} & -- & $\checkmark$ & -- & -- & multi-energy & constr.\ RL \\
Jendoubi and Bouffard~\cite{jendoubi2023hier} & -- & -- & -- & -- & energy mgmt & multi-agent HRL \\
Cui et al.~\cite{cui2024caesbess} & option & -- & -- & -- & TOU / cost & OCTD3 \\
Cui et al.~\cite{cui2025ghtd3} & -- & -- & -- & -- & TOU / cost & GHTD3 \\
Ochoa et al.~\cite{ochoa2024bidding} & -- & -- & -- & -- & DA / RT bidding & multi-agent DRL \\
Pei et al.~\cite{pei2024pricetaker} & -- & -- & -- & -- & spot PT & optimisation \\
\textbf{This work} & $\checkmark$ & $\checkmark$ (adopt) & $\checkmark$ (DAE/FMU) & $\checkmark$ & monthly TOU PT + ETS & PC-HybridTD3 \\
\bottomrule
\end{tabular}
\end{table}

The contributions are three, and no more.
\begin{enumerate}
\item \textbf{Hybrid CAES action (C1).} A parameterized hybrid action formulation is developed for CAES scheduling with disconnected charging, idle, and discharging operating regions. Unlike conventional continuous-action projection, the proposed mode--magnitude representation preserves the intrinsic topology of the CAES control space and avoids the large inactive action regions caused by hard projection.
\item \textbf{Joint feasible support (C2).} Device boxes $\mathcal{A}_T(s)\times\mathcal{A}_B(s)\times\mathcal{A}_C(s)$ are not the legal set: interchange capacity couples the three commands. An analytic decoder first intersects CAES intervals with the grid window (empty modes are masked), then maps thermal and battery heads through the residual intervals conditioned on the chosen $u^{\mathrm{CAES}}$ and $u^{\mathrm{th}}$, so that $a_t\in\mathcal{A}_f(s_t)=(\mathcal{A}_T\times\mathcal{A}_B\times\mathcal{A}_C)\cap\mathcal{A}_{\mathrm{grid}}$. GiveSafe remains a last residual screen; greedy evaluation attempts the actor once and records \texttt{failed\_no\_safe\_action} rather than resampling the same $a^\star$ sixty-four times.
\item \textbf{FMU-in-the-loop evaluation (C3).} An FMU-in-the-loop evaluation framework is established for forecast-aware multi-energy scheduling, where only physically executed transitions are used for economic policy learning. Held-out weekly tests (36/8/8) and an 8760~h deployment study against a price-aware rule, rolling MILP (surrogate optimisation, same-FMU evaluation) and continuous-action TD3 quantify operating cost, renewable curtailment, CAES utilisation and physical safety. Carbon prices, battery wear, CAES start-up, the 24~h forecast, terminal SoC, GiveSafe logging, Gumbel--Softmax and TD3 itself are implementation details, not claimed novelties. FMI is the exchange standard, not a methodological discovery. We do not claim cash dominance over MILP.
\end{enumerate}

Section~\ref{sec:system} describes the plant, monthly TOU path and seasonal boundaries. Section~\ref{sec:problem} formulates physics, the comprehensive objective and the state-dependent hybrid MDP. Section~\ref{sec:method} presents the twin-closed loop and PC-HybridTD3. Section~\ref{sec:results} states the experimental protocol and the ranking rules used for full-week economics. Section~\ref{sec:conclusion} concludes.

% =====================================================================
\section{System description}
\label{sec:system}
% =====================================================================

\subsection{Plant topology and modelling workflow}

Figure~\ref{fig:topology} summarises the multi-energy plant. The supply layer comprises wind, photovoltaic (PV) generation and a grid interface under TOU buy/sell prices. The conversion layer centres on a thermal unit commanded by a continuous load rate $u_{\mathrm{tp}}$ and a CAES train commanded by discrete mode and continuous magnitude. The storage layer comprises a battery with continuous power command $u_{\mathrm{bat}}$ and CAES gas and thermal inventories (gas tank, hot and cold tanks). The demand layer balances electric load and grid exchange.

Plant dynamics follow first-principles multi-physics: thermal ramping and fuel use; electrochemical battery SoC with round-trip efficiency; multi-tank CAES gas storage with thermo-mechanical coupling and mode locks; renewable availability; and nodal power balance with curtailment and unserved residual. The complete Modelica model is constructed and parameterised in \textbf{MWORKS Sysplorer}, then exported as an FMI-compliant FMU. Closed-loop optimisers and learning agents interact with this FMU at a communication interval of one hour, reading plant states and writing hybrid set-points. That compiled object \emph{is} the plant for dispatch: exogenous TOU prices and resource profiles set the market and weather boundary, they do not replace the internal gas--thermal DAEs. FMI is the exchange standard for that interaction, not a claimed novelty.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_topology}
\caption{Topology of the thermal--battery--CAES multi-energy plant. Device and network physics are modelled in MWORKS Sysplorer (Modelica) and exported as an FMI FMU for closed-loop dispatch.}
\label{fig:topology}
\end{figure}
\FloatBarrier

\subsection{Price-taker TOU market}

The plant is a price-taker under Shandong industrial/commercial TOU. Band \emph{windows} and float ratios follow the 2026 State Grid Shandong announcement and Shandong DRC Price [2023] No.~914 (peak $+70\%$, valley $-70\%$, sharp-peak $+100\%$, deep-valley $-90\%$)~\cite{ndrc2021tou,shandong2023tou,shandong2026tou}. Table~\ref{tab:tou-windows} records the five month-groups of the 2026 clock. Absolute buy levels are \emph{not} a year-constant five-tuple. Table~\ref{tab:tou-monthly} transcribes the 110~kV two-part \emph{delivered} energy TOU from the monthly State Grid Shandong agency-purchase tables (Jan--Aug official; Sep--Dec carry the last published month and are labelled scenario-grade). TOU-floating adders (capacity compensation, line-loss, system operation) ride that month's energy base; T\&D energy and funds do not float~\cite{ndrc2023td}. The two-part 110~kV T\&D energy charge is $0.1191$~CNY/kWh through July (third regulatory cycle) and $0.106$~CNY/kWh from 1~August~2026 (fourth cycle; Shandong DRC Price [2026] No.~556)~\cite{shandong2026td}. The hourly series used by the twin is that monthly path unfolded on the official clocks. The flat sell price $0.1875$~CNY/kWh is illustrative (S-grade) and is not claimed as an official monthly delivered tariff. Figure~\ref{fig:price} shows a winter held-out day together with January versus August clocks; Fig.~\ref{fig:boundary} shows weekly wind, irradiance, load and TOU profiles for the three held-out windows (February, May and August on the 2026 calendar). Table~\ref{tab:params} lists plant ratings, legal CAES bands and the remaining market operating points.

\begin{table}[!htbp]
\centering
\caption{2026 Shandong 110~kV two-part agency-purchase delivered TOU (CNY/kWh). Grade O: monthly official table; S: carry-forward of August until published.}
\label{tab:tou-monthly}
\scriptsize
\begin{tabular}{@{}crrrrrrc@{}}
\toprule
Month & Sharp $S$ & Peak $P$ & Flat $F$ & Valley $V$ & Deep $D$ & T\&D & Grade \\
\midrule
1 & 1.1873 & 1.0257 & 0.6488 & 0.2720 & 0.1643 & 0.1191 & O \\
2 & 1.1479 & 0.9936 & 0.6330 & 0.2726 & 0.1696 & 0.1191 & O \\
3 & 1.0648 & 0.9206 & 0.5841 & 0.2477 & 0.1515 & 0.1191 & O \\
4 & 1.0759 & 0.9275 & 0.5811 & 0.2349 & 0.1359 & 0.1191 & O \\
5 & 1.0489 & 0.9090 & 0.5824 & 0.2559 & 0.1626 & 0.1191 & O \\
6 & 1.0303 & 0.8946 & 0.5776 & 0.2608 & 0.1703 & 0.1191 & O \\
7 & 1.1037 & 0.9584 & 0.6190 & 0.2797 & 0.1827 & 0.1191 & O \\
8 & 1.0180 & 0.8844 & 0.5727 & 0.2611 & 0.1720 & 0.1060 & O \\
9--12 & 1.0180 & 0.8844 & 0.5727 & 0.2611 & 0.1720 & 0.1060 & S \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{2026 Shandong industrial/commercial TOU windows by month-group (local hour $h=0,\ldots,23$). Ratios: $S/P/F/V/D=2.0/1.7/1.0/0.3/0.1$~\cite{shandong2023tou,shandong2026tou}.}
\label{tab:tou-windows}
\scriptsize
\begin{tabular}{@{}llp{8.4cm}@{}}
\toprule
Group & Months & Local-hour bands \\
\midrule
Winter-type & Jan, Feb, Dec & $2$--$5$~V; $7$--$8$~P; $10$~V; $11$--$13$~D; $14$~V; $16$--$18$~S; $19$--$20$~P; else F \\
Shoulder & Mar--May & $10$~V; $11$--$13$~D; $14$~V; $17$--$19$~S; $20$--$21$~P; else F \\
June & Jun & $7$--$11$~V; $16$~P; $17$--$21$~S; $22$~P; else F \\
Summer & Jul--Aug & $1$--$5$~V; $16$~P; $17$--$21$~S; $22$~P; else F \\
Autumn & Sep--Nov & $10$~V; $11$--$13$~D; $14$~V; $16$~P; $17$--$18$~S; $19$--$20$~P; else F \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Plant and market parameters used in the weekly case study.}
\label{tab:params}
\scriptsize
\begin{tabular}{@{}lll@{}}
\toprule
Quantity & Value & Note \\
\midrule
Wind / PV nameplate & 300 / 260~MW & Resource-limited injection \\
Thermal \(P_{\min}\)--\(P_{\mathrm{cap}}\) & 50--150~MW & Load rate \(u_{\mathrm{tp}}\in[1/3,1]\); no shut-down \\
Battery power / energy & 100~MW / 500~MWh & \(\eta=0.85\) on discharge \\
CAES electric rating & 150~MW & Legal \(u_{\mathrm{caes}}\in[-1,-0.33]\cup\{0\}\cup[0.86,1]\) \\
Grid hard / contract limits & \(\pm500\) / \(\pm200\)~MW & Soft contract priced in \(J^{\mathrm{gen}}\) \\
TOU buy & monthly 110~kV two-part, Table~\ref{tab:tou-monthly} & Official windows/ratios; no year-constant five-tuple \\
TOU sell & 0.1875~CNY/kWh & Illustrative flat export (S-grade; not official monthly delivered) \\
ETS price \(\pi_{\mathrm{CO_2}}\) & 97.49~CNY/tCO\(_2\) & 2024 YE national CEA close~\cite{mee2025etsnews,mee2025etsreport} \\
Thermal EF / grid EF / benchmark \(\beta\) & 0.85 / 0.6191 / 0.8049~t/MWh & Scenario EF; Shandong 2023 EF; Class~II 2024 \(\beta\)~\cite{mee2023ef,mee2024quota} \\
Curtailment / unserved & 300 / 1000~CNY/MWh & Objective penalties; \(\approx\)GHTD3 \(\nu_{\mathrm{res}}\)~\cite{cui2025ghtd3} \\
CAES startup (scaled) & \(\approx 4617\)~CNY/event & Cui 2024 \(3.42\)~USD@800~kW + linear \(P\) extrapolation~\cite{cui2024caesbess} \\
Grid-contract excess & 600~CNY/MWh beyond \(\pm200\)~MW & Scenario soft band (not statutory) \\
Episode length \(T\) & 168~h & Weekly MDP; calendar only rotates week starts \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.68\textwidth]{fig_price_tou}
\caption{Purchase TOU on the monthly 110~kV two-part path. (a)~First day of the winter held-out week (official Jan--Feb clocks). (b)~January versus August on the same 24~h clock: band windows differ by month-group and the flat level drops after the August T\&D revision.}
\label{fig:price}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_seasonal_boundary}
\caption{Weekly wind speed, solar irradiance, electric load and monthly 110~kV TOU purchase price on the three held-out evaluation windows (winter week~5 / February, transition week~18 / May, summer week~31 / August). Irradiance is W/m$^2$, not normalised.}
\label{fig:boundary}
\end{figure}
\FloatBarrier

% =====================================================================
\section{Problem formulation}
\label{sec:problem}
% =====================================================================

This section formulates the multi-energy plant physics, operating constraints and market objective that define the \emph{weekly} scheduling problem (\(T=168\)~h). Continuous-time differential--algebraic equations of the plant (thermal unit, battery, multi-tank CAES, renewables and grid interface) are integrated in the Sysplorer-exported FMU with communication interval $\tau_s=1$~h; the relations below are the dispatch-level models realised by that twin and used by optimisers and learning agents. The 8760~h plant calendar only rotates weekly episode starts; it is not an annual MDP. We adopt the following sign convention throughout: renewable and thermal generation are non-negative when injecting power; battery and CAES charge is positive and discharge is negative; grid import is positive. Component sets are
\begin{equation}
\begin{aligned}
I_1&=\{\mathrm{w},\mathrm{pv}\},\quad
I_2=\{\mathrm{th},\mathrm{caes}\},\quad
I_3=\{\mathrm{bat},\mathrm{gas},\mathrm{hot},\mathrm{cold}\},\\
I&=I_1\cup I_2\cup I_3.
\end{aligned}
\label{eq:sets}
\end{equation}

\subsection{Energy generation units}

\subsubsection{Photovoltaics}

PV available power depends on plane-of-array irradiance $G_t$, module surface temperature $T_t^{\mathrm{pv}}$ and nameplate capacity $P_n^{\mathrm{pv}}=260$~MW. With standard irradiance $G_{\mathrm{stc}}=1000$~W/m$^2$, standard temperature $T_{\mathrm{stc}}=298.15$~K, temperature coefficient $K_T=0.005$~K$^{-1}$ and conversion efficiency $\eta^{\mathrm{pv}}=0.95$,
\begin{equation}
P_t^{\mathrm{pv,avail}}
=
\max\Bigl(
P_n^{\mathrm{pv}}
\frac{G_t}{G_{\mathrm{stc}}}
\bigl[1-K_T\bigl(T_t^{\mathrm{pv}}-T_{\mathrm{stc}}\bigr)\bigr]
\eta^{\mathrm{pv}},\,0\Bigr).
\label{eq:pv}
\end{equation}
Module temperature is obtained from ambient temperature $T_t^{\mathrm{air}}$, wind speed $v_t$ and irradiance by the twin correlation
\begin{equation}
T_t^{\mathrm{pv}}
=
T_t^{\mathrm{air}}
+
0.0138\bigl(1+0.031(T_t^{\mathrm{air}}-273.15)\bigr)
\bigl(1-0.042\,v_t\bigr)G_t.
\label{eq:pv-temp}
\end{equation}
Actual injected PV power after bus curtailment satisfies $0\le P_t^{\mathrm{pv}}\le P_t^{\mathrm{pv,avail}}$.

\subsubsection{Wind turbines}

Wind available power follows a cubic power curve with cut-in, rated and cut-out speeds $v_{\mathrm{ci}}=3$~m/s, $v_{\mathrm{r}}=10$~m/s and $v_{\mathrm{co}}=15$~m/s on capacity $P_n^{\mathrm{w}}=300$~MW:
\begin{equation}
P_t^{\mathrm{w,avail}}
=
\begin{cases}
0, & 0\le v_t < v_{\mathrm{ci}}\ \text{or}\ v_t\ge v_{\mathrm{co}},\\[2pt]
P_n^{\mathrm{w}}\,\dfrac{v_t^{3}-v_{\mathrm{ci}}^{3}}{v_{\mathrm{r}}^{3}-v_{\mathrm{ci}}^{3}},
& v_{\mathrm{ci}}\le v_t < v_{\mathrm{r}},\\[6pt]
P_n^{\mathrm{w}}, & v_{\mathrm{r}}\le v_t < v_{\mathrm{co}}.
\end{cases}
\label{eq:wt}
\end{equation}
Turbine pitch is not a free decision variable. Actual wind injection obeys $0\le P_t^{\mathrm{w}}\le P_t^{\mathrm{w,avail}}$.

\subsection{Energy conversion units}

\subsubsection{Thermal generation}

Thermal electric power is commanded by the continuous load rate $u_{\mathrm{tp},t}$:
\begin{equation}
P_t^{\mathrm{th}} = u_{\mathrm{tp},t}\, P_{\mathrm{cap}}^{\mathrm{th}},
\qquad
u_{\min}\le u_{\mathrm{tp},t}\le u_{\max},
\label{eq:th}
\end{equation}
with $P_{\mathrm{cap}}^{\mathrm{th}}=150$~MW, $P_{\min}^{\mathrm{th}}=50$~MW, $u_{\min}=P_{\min}^{\mathrm{th}}/P_{\mathrm{cap}}^{\mathrm{th}}=1/3$ and $u_{\max}=1$ (no shut-down). Inter-hour ramping is enforced on the actual thermal power,
\begin{equation}
\bigl|P_t^{\mathrm{th}}-P_{t-1}^{\mathrm{th}}\bigr|
\le
\Delta_{\mathrm{th}}^{\max}
=
r_{\max}\,P_{\mathrm{cap}}^{\mathrm{th}}\,\tau_s,
\label{eq:th-ramp}
\end{equation}
where $r_{\max}=0.0025/60$~s$^{-1}$ is the Modelica load-rate limit (equivalently $0.15$~p.u.\ per hour). Fuel and O\&M costs enter $C_t^{\mathrm{op}}$ in~\eqref{eq:dJ}.

\subsubsection{CAES conversion}

CAES is a hybrid conversion--storage plant. The hybrid command comprises a discrete mode and continuous magnitude
\begin{equation}
m_{\mathrm{caes},t}\in\{\mathrm{charge},\mathrm{idle},\mathrm{discharge}\},\qquad
\mu_{\mathrm{caes},t}\in[0,1],
\label{eq:caes-cmd}
\end{equation}
mapped to a signed electric power command on capacity $P_{\mathrm{cap}}^{\mathrm{caes}}=150$~MW,
\begin{equation}
P_t^{\mathrm{caes}}
=
\begin{cases}
+\mu_{\mathrm{caes},t}\,P_{\mathrm{cap}}^{\mathrm{caes}}, & m=\mathrm{charge},\\
0, & m=\mathrm{idle},\\
-\mu_{\mathrm{caes},t}\,P_{\mathrm{cap}}^{\mathrm{caes}}, & m=\mathrm{discharge},
\end{cases}
\label{eq:caes-p}
\end{equation}
or equivalently $P_t^{\mathrm{caes}}=u_{\mathrm{caes},t}P_{\mathrm{cap}}^{\mathrm{caes}}$ with the non-convex legal set
\begin{equation}
u_{\mathrm{caes},t}
\in
[-1,-0.33]\cup\{0\}\cup[0.86,1]
\label{eq:caes-legal}
\end{equation}
encoding minimum compressor/expander loading. Mode locks and minimum-run dwell constraints further restrict admissible transitions of $m_{\mathrm{caes}}$. Electric power is realised as planned ($P^{\mathrm{act}}=P^{\mathrm{plan}}$); mass-flow set-points of air and thermal fluid scale with $|P_t^{\mathrm{caes}}|$ and reverse direction between compression and expansion.

\subsection{Energy storage units}

\subsubsection{Battery}

Battery power is commanded by $u_{\mathrm{bat},t}\in[-1,1]$ on $P_{\mathrm{cap}}^{\mathrm{bat}}=100$~MW:
\begin{equation}
P_t^{\mathrm{bat}} = u_{\mathrm{bat},t}\, P_{\mathrm{cap}}^{\mathrm{bat}}.
\label{eq:bat-p}
\end{equation}
With energy capacity $E_{\mathrm{cap}}=500$~MWh and discharge efficiency $\eta=0.85$ (charge path lossless as in the twin), the SoC dynamics are
\begin{equation}
\frac{\mathrm{d}s_{\mathrm{bat}}}{\mathrm{d}t}
=
\begin{cases}
P^{\mathrm{bat}}/E_{\mathrm{cap}}, & P^{\mathrm{bat}}\ge 0,\\[2pt]
P^{\mathrm{bat}}/(E_{\mathrm{cap}}\,\eta), & P^{\mathrm{bat}}<0,
\end{cases}
\label{eq:bat-soc}
\end{equation}
or, in hourly form with $[x]_+=\max(x,0)$ and $[x]_-=\max(-x,0)$,
\begin{equation}
s_{t+1,\mathrm{bat}}
=
s_{t,\mathrm{bat}}
+
\tau_s\frac{[P_t^{\mathrm{bat}}]_+ - [P_t^{\mathrm{bat}}]_-/\eta}{E_{\mathrm{cap}}}.
\label{eq:bat-soc-d}
\end{equation}
Inventory bounds are $s_{\mathrm{bat}}\in[0.1,0.9]$.

\subsubsection{CAES inventories}

CAES energy is stored as compressed air and two thermal fluid tanks. Gas SoC is pressure-based with rating $p_{\mathrm{norm}}=100$~bar,
\begin{equation}
s_{t,\mathrm{gas}}
=
\frac{p_t}{p_{\mathrm{norm}}},
\qquad
\underline{s}_{\mathrm{gas}}\le s_{t,\mathrm{gas}}\le \overline{s}_{\mathrm{gas}},
\label{eq:gas-soc}
\end{equation}
with $(\underline{s}_{\mathrm{gas}},\overline{s}_{\mathrm{gas}})=(0.6,1.0)$. Fixed-volume gas-tank mass and energy balances (ideal-air medium) evolve pressure $p$ and specific enthalpy $h$ under port mass flows $\dot m_{\mathrm{a}},\dot m_{\mathrm{b}}$ and wall heat $q_{\mathrm{w}}$,
\begin{align}
V\bigl(\partial_p\rho\,\dot p+\partial_h\rho\,\dot h\bigr)
&=
\dot m_{\mathrm{a}}+\dot m_{\mathrm{b}},
\label{eq:gas-mass}\\
V\bigl(-\dot p+\partial_p\rho\,\dot p\,h+\partial_h\rho\,\dot h\,h+\rho\dot h\bigr)
&=
\dot m_{\mathrm{a}}h_{\mathrm{a}}^{\mathrm{in}}
+
\dot m_{\mathrm{b}}h_{\mathrm{b}}^{\mathrm{out}}
+
q_{\mathrm{w}},
\label{eq:gas-energy}
\end{align}
with $q_{\mathrm{w}}=\alpha_{\mathrm{w}}S(T_{\mathrm{w}}-T)$ and gas temperature $T=T(p,h)$ (clipped at $253$~K). Hot and cold tanks store water at fixed pressure with free surface area $A$ and total volume $V_0=1.5\times10^{4}$~m$^3$. Level-based inventories
\begin{equation}
s_{t,i}
=
\frac{\ell_{t,i}}{V_0/A},
\qquad
i\in\{\mathrm{hot},\mathrm{cold}\},
\label{eq:thermal-soc}
\end{equation}
obey mass and enthalpy balances when within $(\underline{s}_i,\overline{s}_i)=(0.05,0.95)$,
\begin{equation}
\dot m_i=\dot m_{\mathrm{a},i}+\dot m_{\mathrm{b},i},\qquad
m_i\dot h_i
=
\dot m_{\mathrm{a},i}h_{\mathrm{a},i}^{\mathrm{in}}
+
\dot m_{\mathrm{b},i}h_{\mathrm{b},i}^{\mathrm{out}}+Q_{\mathrm{gen},i}.
\label{eq:tank-bal}
\end{equation}
Compression ($P^{\mathrm{caes}}>0$) charges the gas inventory and drives the thermal circuit one way; expansion ($P^{\mathrm{caes}}<0$) reverses the flows. Primary weekly recovery inventories are $s_{\mathrm{bat}}$ and $s_{\mathrm{gas}}$; hot/cold SoCs and temperatures $(T^{\mathrm{hot}},T^{\mathrm{cold}})$ are process states used in soft shaping. All inventories obey~\eqref{eq:socbox}.

\subsection{System operation constraints}

The electric bus closes the power balance with curtailment and unserved load. Let $P_t^{\mathrm{w,avail}},P_t^{\mathrm{pv,avail}}$ be renewable availability and $P_t^{\mathrm{load,plan}}$ the scheduled demand. Residual power after dispatchable assets is absorbed first by curtailing wind, then PV; residual deficit is met by demand response down to $20\%$ of plan, with remainder counted as unserved:
\begin{equation}
\begin{aligned}
&P_t^{\mathrm{th}}+P_t^{\mathrm{bat}}+P_t^{\mathrm{caes}}
+P_t^{\mathrm{w}}+P_t^{\mathrm{pv}}+P_t^{\mathrm{grid}}
=
P_t^{\mathrm{load}},\\
&0\le P_t^{\mathrm{w}}\le P_t^{\mathrm{w,avail}},\quad
0\le P_t^{\mathrm{pv}}\le P_t^{\mathrm{pv,avail}},\\
&P_t^{\mathrm{curt}}\ge 0,\quad
P_t^{\mathrm{uns}}\ge 0,\quad
0.2\,P_t^{\mathrm{load,plan}}\le P_t^{\mathrm{load}}\le P_t^{\mathrm{load,plan}}.
\end{aligned}
\label{eq:balance}
\end{equation}
Here $P_t^{\mathrm{curt}}$ is residual surplus after wind-then-PV curtailment and $P_t^{\mathrm{uns}}$ is residual shortfall after load reduction. Box and ramp limits, inventory bounds and grid exchange limits are
\begin{equation}
\underline{P}_{i}\le P_t^{i}\le \overline{P}_{i},\qquad
\bigl|P_t^{i}-P_{t-1}^{i}\bigr|\le \Delta_i^{\max},
\quad i\in\{\mathrm{th},\mathrm{bat}\},
\label{eq:box}
\end{equation}
\begin{equation}
\underline{s}_{i}\le s_{t,i}\le \overline{s}_{i},\qquad i\in\{\mathrm{bat},\mathrm{gas},\mathrm{hot},\mathrm{cold}\},
\label{eq:socbox}
\end{equation}
\begin{equation}
P_{\mathrm{grid}}^{\min}\le P_t^{\mathrm{grid}}\le P_{\mathrm{grid}}^{\max},
\label{eq:grid}
\end{equation}
with $(P_{\mathrm{grid}}^{\min},P_{\mathrm{grid}}^{\max})=(-500,500)$~MW. Actions that would violate~\eqref{eq:balance}--\eqref{eq:grid},~\eqref{eq:caes-legal} or CAES mode locks lie outside the dynamic feasible set $\mathcal{F}(s_t)$.

Weekly operation records an optional energy-SoC recovery score on primary inventories, following the soft indicator of~\cite{cui2025ghtd3} rather than a hard action override or feasibility veto,
\begin{equation}
L_1^{\mathrm{e}}
= \bigl|s_{T,\mathrm{bat}}-s_{0,\mathrm{bat}}\bigr|
+ \bigl|s_{T,\mathrm{gas}}-s_{0,\mathrm{gas}}\bigr|
\le \varepsilon,
\label{eq:soc}
\end{equation}
with $\varepsilon=0.06$. Operating boxes remain $s_{\mathrm{bat}}\in[0.1,0.9]$. The battery command is never rewritten at week-end;~\eqref{eq:soc} only gates the terminal bonus in~\eqref{eq:term} and is not used to rank policies.

\subsection{Optimization objectives}

Let $t=0,\ldots,T-1$ index hours in a weekly episode ($T=168$). Grid purchase/sale energies are $E_t^{\mathrm{buy}}=\tau_s[P_t^{\mathrm{grid}}]_+$ and $E_t^{\mathrm{sell}}=\tau_s[P_t^{\mathrm{grid}}]_-$ (MWh). Device operating costs $C_t^{\mathrm{op}}$ aggregate thermal fuel/O\&M and storage energy-bookkeeping cash increments exported by the twin (battery cycle life is not in Modelica income; see $C_t^{\mathrm{deg}}$). Under price-taker TOU prices $\lambda_t^{\mathrm{buy}},\lambda_t^{\mathrm{sell}}$, the hourly \emph{cash} increment is
\begin{equation}
\Delta J_t^{\mathrm{cash}}
=
\lambda_t^{\mathrm{sell}} E_t^{\mathrm{sell}}
-
\lambda_t^{\mathrm{buy}} E_t^{\mathrm{buy}}
-
C_t^{\mathrm{op}}.
\label{eq:dJ}
\end{equation}
Step CO$_2$ mass uses emission factors $\eta_{\mathrm{th}},\eta_{\mathrm{grid}}$ (tCO$_2$/MWh) on thermal energy and grid imports,
\begin{equation}
\Delta m_t
=
\eta_{\mathrm{th}} E_t^{\mathrm{th}}
+
\eta_{\mathrm{grid}} E_t^{\mathrm{buy}},
\label{eq:co2}
\end{equation}
with carbon priced at the reproducible 2024 national ETS year-end composite close $\pi_{\mathrm{CO_2}}=97.49$~CNY/tCO$_2$e (2024 band $69$--$106$, peak $105.65$)~\cite{mee2025etsnews,mee2025etsreport}. Sensitivity retains the legacy operating point $80$~\cite{icap2025chinaets} and a GHTD3 literature cross-check $\approx 86.4$~CNY/t ($0.012$~USD/kg at $7.2$~CNY/USD)~\cite{cui2025ghtd3,worldbank2024usdnyc}. The default Python settlement uses an intensity-benchmark mode: thermal output accumulates an intensity allowance $A_t=\beta E_t^{\mathrm{th}}$ against emissions $E_t=\eta_{\mathrm{th}}E_t^{\mathrm{th}}$ and settles the position $Q=A-E$ at episode end, while grid-import CO$_2$ remains a step charge $\pi_{\mathrm{CO_2}}\eta_{\mathrm{grid}}E_t^{\mathrm{buy}}$. Here $\beta=0.8049$~t/MWh is the official 2024 Class~II (conventional coal, this plant's $150$~MW rating) \emph{generation benchmark}~\cite{mee2024quota}, $\eta_{\mathrm{grid}}=0.6191$~t/MWh is the official 2023 Shandong location-based electricity factor~\cite{mee2023ef}, and $\eta_{\mathrm{th}}=0.85$~t/MWh is a plant direct-emission \emph{scenario} factor (not identical to $\beta$). A flat step tax $\pi_{\mathrm{CO_2}}\Delta m_t$ remains available for ablation and unit tests. Carbon is treated as an external ETS-style charge rather than a second fuel bill. Curtailment and unserved energies $E_t^{\mathrm{curt}},E_t^{\mathrm{uns}}$ follow bus residuals and enter a Cui-style spill cost~\cite{cui2025ghtd3},
\begin{equation}
C_t^{CUT}
=
\nu_{\mathrm{curt}} E_t^{\mathrm{curt}}
+
\nu_{\mathrm{uns}} E_t^{\mathrm{uns}},
\label{eq:cut}
\end{equation}
with operating points $\nu_{\mathrm{curt}}=300$~CNY/MWh (cross-checked against GHTD3 $\nu_{\mathrm{res}}=0.041$~USD/kWh~$\approx 295$~CNY/MWh) and $\nu_{\mathrm{uns}}=1000$~CNY/MWh as a conservative unserved penalty well below typical VOLL survey ranges~\cite{schroeder2015voll}---objective coefficients, not regulated tariffs. Battery \emph{cycle} cost is accounted outside pure energy bookkeeping: device-level battery income in the plant model reflects energy transfer only, whereas capital wear is represented by a convex cumulative-discharge term $\psi(\delta)=a_0\delta^{p}$ with $p\approx 2.03$~\cite{cui2025ghtd3,cui2024caesbess}. We calibrate $a_0$ so that lifetime discharge exhausts capital cost (LCOS-style amortisation~\cite{xu2022lcos}). Let $\delta_t$ be episode cumulative \emph{discharge} energy (MWh; charge does not increase $\delta$) and $\delta_0=\rho E_{\mathrm{life}}$ an optional mid-life offset. On the weekly protocol used here we set $\rho=0$, so wear starts from a zero discharge tally; a positive offset is reserved for continuous-year books. With lifetime discharge $E_{\mathrm{life}}=N_{\mathrm{cycle}}\mathrm{DoD}_{\mathrm{eq}}E_{\mathrm{cap}}$ and total capital $\mathrm{Capex}=c_{\mathrm{kWh}}E_{\mathrm{cap}}$,
\begin{equation}
\psi(\delta)=a_0\,\delta^{p},\quad
a_0=\frac{\mathrm{Capex}}{E_{\mathrm{life}}^{p}},\quad
p=2.03,\quad
C_t^{\mathrm{deg}}
=
\psi(\delta_0+\delta_t)-\psi(\delta_0+\delta_{t-1}),
\label{eq:deg}
\end{equation}
hence $\psi(E_{\mathrm{life}})=\mathrm{Capex}$ and degradation becomes strictly more expensive as cumulative discharge grows. Default plant-scale numbers match the battery capacity ($E_{\mathrm{cap}}=500$~MWh) with a stationary LFP-class economic point $c_{\mathrm{kWh}}=1000$~CNY/kWh, $N_{\mathrm{cycle}}=5000$ and $\mathrm{DoD}_{\mathrm{eq}}=0.8$~\cite{xu2022lcos}. CAES mode switches incur a Cui Table~2 start-up coefficient $c_{\mathrm{su}}=3.42$~USD at $800$~kW~\cite{cui2024caesbess}, converted at $7.2$~CNY/USD~\cite{worldbank2024usdnyc}; scaling linearly to the plant $P_{\mathrm{cap}}=150$~MW is an explicit \emph{extrapolation} (sensitivity: unscaled / linear / $\sqrt{P}$). Soft interchange-contract excess beyond a scenario band $P_{\lim}=\pm200$~MW is priced as
\begin{equation}
C_t^{\mathrm{grid}}
=
\nu_{\mathrm{grid}}\max\bigl(0,\,|P_t^{\mathrm{grid}}|-P_{\lim}\bigr)\Delta t,
\label{eq:grid-contract}
\end{equation}
with scenario $\nu_{\mathrm{grid}}=600$~CNY/MWh (not a statutory fee; the FMU hard interchange remains $\pm500$~MW). The comprehensive monetary objective is
\begin{equation}
\Delta J_t^{\mathrm{gen}}
=
\Delta J_t^{\mathrm{cash}}
-
C_t^{\mathrm{CO_2}}
-
C_t^{CUT}
-
C_t^{\mathrm{deg}}
-
C_t^{\mathrm{su}}
-
C_t^{\mathrm{grid}},
\qquad
\max_{\{a_t\}}\; J^{\mathrm{gen}} = \sum_{t=0}^{T-1} \Delta J_t^{\mathrm{gen}}
\quad\text{s.t.}\quad
\eqref{eq:pv}\text{--}\eqref{eq:grid},\
a_t\in\mathcal{F}(s_t).
\label{eq:J}
\end{equation}
Primary economic ranking uses the full-week comprehensive cost $CC=-J^{\mathrm{gen}}$ (equivalently maximising $J^{\mathrm{gen}}$). Hour-scale hybrid actions thus couple to week-scale inventory motion, carbon exposure, renewable spill, storage wear, CAES startups and contracted interchange. Policies are ranked by calibrated episode reward under~\eqref{eq:J} (Section~\ref{sec:reward});~\eqref{eq:soc} is a soft bonus diagnostic only.

\subsection{State-dependent hybrid-action MDP}
\label{sec:pamdp}

Closed-loop control is a forecast-aware receding decision policy, not a myopic box TD3 and not $s_t\to g_t\to a_t$. The MDP is $\mathcal{M}=\langle\mathcal{S},\mathcal{A}_f(\cdot),P,r,\gamma\rangle$ with $\gamma=0.99$. The state is
\begin{equation}
s_t=[x_t,f_t,p_t],
\label{eq:state}
\end{equation}
where $x_t$ is the FMU physical vector (battery / gas / hot / cold SoC, gas pressure and temperatures, thermal / battery / CAES / grid / wind / PV / load / curtailment / unserved powers; 19 channels in \texttt{env\_config.yaml}), $f_t$ is the 24~h-ahead forecast $\{\hat v,\hat G,\hat T_a,\hat P_{\mathrm{load}}\}_{t:t+23}$, and $p_t$ is the TOU path $\{\lambda^{\mathrm{buy}},\lambda^{\mathrm{sell}}\}_{t:t+23}$. The reported observation dimension is $166$. The hybrid action is
\begin{equation}
a_t
=
\bigl(u_t^{\mathrm{th}},\, u_t^{\mathrm{bat}},\, m_t,\, z_t\bigr),
\qquad
m_t\in\{D,I,C\},\quad
z_t\in[0,1],
\label{eq:action-hyb}
\end{equation}
with support
\begin{equation}
\mathcal{A}_f(s)
=
\mathcal{A}_{\mathrm{tp}}(s)\times
\mathcal{A}_{\mathrm{bat}}(s)\times
\bigcup_{k\in\mathcal{K}(s)}
\bigl(\{k\}\times\mathcal{M}_k(s)\bigr).
\label{eq:support}
\end{equation}
Here $\mathcal{K}(s)\subseteq\{\mathrm{dis},\mathrm{idle},\mathrm{chg}\}$ is the admissible mode set from mode locks and inventory margins, and $\mathcal{M}_k(s)=[\underline{u}_k(s),\overline{u}_k(s)]$ is the dynamic magnitude interval for mode~$k$ returned by the feasibility oracle (twin physics plus engineered margins; sensitivity in Section~\ref{sec:results} separates the two). Idle is a point mass (no continuous magnitude). The physical FMU command is the continuous triple
\begin{equation}
a_t = \bigl(u_{\mathrm{tp},t},\, u_{\mathrm{bat},t},\, u_{\mathrm{caes},t}\bigr),
\label{eq:action}
\end{equation}
decoded from the \emph{current} interval rather than from a static device band alone,
\begin{equation}
u_t^{\mathrm{CAES}}
=
\begin{cases}
l_D(s_t)+z_t\bigl[h_D(s_t)-l_D(s_t)\bigr], & m=D,\\
0, & m=I,\\
l_C(s_t)+z_t\bigl[h_C(s_t)-l_C(s_t)\bigr], & m=C.
\end{cases}
\label{eq:decode}
\end{equation}
When margins vanish,~\eqref{eq:decode} collapses to the device envelope~\eqref{eq:caes-legal}. Boolean mask $M(s)\in\{0,1\}^3$ encodes $\mathcal{K}(s)$. Transitions integrate~\eqref{eq:pv}--\eqref{eq:tank-bal} for one plant hour under $a_t\in\mathcal{F}(s_t)$ only. Figure~\ref{fig:caes-legal} places the disconnected device envelope in the main text as an operating constraint, not as a methodological discovery.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.88\textwidth]{fig_caes_legal}
\caption{Disconnected legal envelope of the CAES electric command $u_{\mathrm{caes}}$ (discharge band, idle, charge band) together with mode locks that shrink $\mathcal{K}(s)$ and $\mathcal{M}_k(s)$.}
\label{fig:caes-legal}
\end{figure}

\subsection{Reward function for learning}
\label{sec:reward}

With calibration constant $C_{\mathrm{ref}}\approx1.565\times10^{5}$~CNY (rule-controller $p_{95}$ step cash magnitude),
\begin{equation}
r_t^{\mathrm{econ}} = \frac{\Delta J_t^{\mathrm{gen}}}{C_{\mathrm{ref}}}.
\label{eq:recon}
\end{equation}
Potential-based inventory shaping uses the weighted L1 distance $L_{1,t}$ of (battery, gas, hot, cold) relative to episode start:
\begin{equation}
r_t^{\mathrm{shape}}
= \kappa_t \bigl(L_{1,t-1}-L_{1,t}\bigr)
- \kappa_t^{\mathrm{abs}} L_{1,t},
\label{eq:shape}
\end{equation}
with base $\kappa=5$ and a recovery-window scale-up over the last $H=40$~h. At a completed, failure-free terminal step the Cui-style indicator is
\begin{equation}
r_T^{\mathrm{term}}
=
\begin{cases}
B, & L_1^{\mathrm{e}}\le\varepsilon,\\
0, & \text{otherwise},
\end{cases}
\label{eq:term}
\end{equation}
with $B=15$. Missing~\eqref{eq:soc} costs the bonus only; it does not apply a proportional fail penalty, does not rewrite actions and does not invalidate the week. The extrinsic reward is the sum of the three terms
\begin{equation}
r_t^{\mathrm{ext}}
= r_t^{\mathrm{econ}}+r_t^{\mathrm{shape}}+r_t^{\mathrm{term}}.
\label{eq:rext}
\end{equation}
Classical programmes must convexify~\eqref{eq:caes-legal} and the CAES thermo-mechanical coupling; PC-HybridTD3 (Section~\ref{sec:method}) optimises~\eqref{eq:J} by closed-loop interaction with the plant twin under support~\eqref{eq:support}. Rolling MILP optimises a linear energy surrogate and is evaluated on the same FMU.

% =====================================================================
\section{Solution methodology}
\label{sec:method}
% =====================================================================

\subsection{Same-timescale hybrid policy}
\label{sec:same-timescale}

The solver is a same-timescale \emph{physics-constrained parameterized hybrid TD3}~\cite{fujimoto2018td3,fan2019hybrid}: one plant reward, one state-dependent support $\mathcal{A}_f(s)$, one adopted GiveSafe screen on the DAE twin. Mode and magnitude are decided jointly at every one-hour step. The research question is the current hybrid support, not a $c$-step inventory hierarchy and not a maximum-entropy SAC. PC-HybridTD3 is a two-headed deterministic actor (discrete mode head plus a single magnitude head) whose decoded command already lies in $\mathcal{A}_f(s)$. When independent slow inventory targets, start-up costs or long-duration options dominate, hierarchical or option methods remain reasonable alternatives.

\subsection{Twin-closed loop}
\label{sec:twin-loop}
Before every plant hour the controller builds joint $\mathcal{A}_f(s)$ (device boxes intersected with interchange), decodes $\pi_{\theta}(s,\mathcal{A}_f(s))$ (greedy $\arg\max$ mode at evaluation), and offers the physical triple to GiveSafe. Only an accepted command steps the FMU; the physical transition enters $\mathcal{D}_{\mathrm{B}}$. Rejections never advance the twin and are written only to a safety audit set $\mathcal{D}_{\mathrm{S}}$. Stochastic training may resample up to $N_{\mathrm{try}}=64$ times; greedy evaluation tries once and records \texttt{failed\_no\_safe\_action} rather than aborting the training job. Training warms up with $N_{\mathrm{warm}}=1024$ random-feasible physical hours (legal modes only; no idle logit bias); if both charge and discharge are available in the oracle yet $N_D\le 100$ or $N_C\le 100$, training stops before any gradient step. Seasonal runs keep GiveSafe on the oracle and disable shadow-FMU validation.

Figure~\ref{fig:algorithm} summarises the loop. Figure~\ref{fig:action-rep} contrasts projection, static-band hybrid decoding and dynamic-support decoding.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_algorithm}
\caption{PC-HybridTD3 closed loop on the Sysplorer FMU. The actor decodes $(m,z)$ onto $\mathcal{A}_f(s)$. GiveSafe (adopted, fallback off) admits the hour; accepted transitions enter $\mathcal{D}_{\mathrm{B}}$, rejections enter $\mathcal{D}_{\mathrm{S}}$ only and never the economic Bellman update.}
\label{fig:algorithm}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\textwidth]{fig_action_rep}
\caption{Action representation used by the two ablations and by PC-HybridTD3. Left: continuous projection onto~\eqref{eq:caes-legal} can mute charge or discharge into idle. Centre: static-band hybrid decoding places magnitude on the device envelope and clamps after sampling. Right: PC-HybridTD3 maps magnitude through the current interval $\mathcal{M}_k(s)$ so that $a_t\in\mathcal{A}_f(s_t)$ before GiveSafe.}
\label{fig:action-rep}
\end{figure}
\FloatBarrier

\subsection{Parameterized hybrid actor}
\label{sec:hybrid-sac}

The actor emits
\begin{equation}
a_t
=
\bigl(u_t^{\mathrm{th}},\,u_t^{\mathrm{bat}},\,m_t,\,z_t\bigr),
\qquad
m_t\in\{D,I,C\},\quad
z_t\in[0,1].
\label{eq:pi-factor}
\end{equation}
Thermal and battery heads map bounded latents by a sigmoid onto the current boxes $\mathcal{A}_{\mathrm{tp}}(s)$ and $\mathcal{A}_{\mathrm{bat}}(s)$. The CAES head emits three mode logits $\ell\in\mathbb{R}^{3}$ and one magnitude logit. With Boolean mask $M(s)\in\{0,1\}^3$,
\begin{equation}
\tilde\ell
=
\ell + \log\bigl(M(s)+\varepsilon\bigr)
\quad\text{(illegal modes set to $-\infty$)},
\label{eq:mask}
\end{equation}
so $\mathrm{softmax}(\tilde\ell)$ is supported only on $\mathcal{K}(s)$. Evaluation and the TD3 target take $m'=\arg\max_m\tilde\ell(s')$. The physical CAES command is decoded on the \emph{current} oracle intervals,
\begin{equation}
u^{\mathrm{CAES}}
=
\begin{cases}
\underline{u}_D(s)+z\bigl(\overline{u}_D(s)-\underline{u}_D(s)\bigr), & m=D,\\
0, & m=I,\\
\underline{u}_C(s)+z\bigl(\overline{u}_C(s)-\underline{u}_C(s)\bigr), & m=C,
\end{cases}
\label{eq:affine}
\end{equation}
which is~\eqref{eq:decode}. Idle is a point mass. Device boxes are then coupled through interchange. With $P_{\mathrm{grid}}=P_T u^{\mathrm{th}}-P_B u^{\mathrm{bat}}-P_C u^{\mathrm{CAES}}-R_t$, empty CAES modes after intersecting $\mathcal{M}_k(s)$ with the grid window are masked; thermal is remapped onto the residual interval given $u^{\mathrm{CAES}}$; battery is remapped given $(u^{\mathrm{th}},u^{\mathrm{CAES}})$. GiveSafe is a last residual check: greedy evaluation attempts the actor once and records \texttt{failed\_no\_safe\_action} instead of repeating $a^\star$ sixty-four times. A post-hoc clamp on $u^{\mathrm{CAES}}$ is an audit, not the policy.

The discrete mode is not differentiable. Actor updates therefore use Straight-Through Gumbel--Softmax: a hard one-hot in the forward pass and a soft gradient in the backward pass, with temperature annealed $\tau_{\mathrm{G}}:1.0\to 0.2$. No extra entropy temperatures, option policies or hierarchical initiation sets are introduced.

\subsection{Hybrid critic and TD3 target}
\label{sec:critic}

Twin critics $Q_{\phi_1},Q_{\phi_2}$ take the six-dimensional packing
$(u^{\mathrm{th}},u^{\mathrm{bat}},e_m,z)$
rather than a collapsed scalar $u_{\mathrm{caes}}$ on $[-1,1]$. The forbidden gap between the discharge band, idle and the charge band is therefore not an interpolation axis. Writing $Q=\min(Q_{\phi_1},Q_{\phi_2})$ and clipping $Q$ at $\pm 200$, the Bellman target is the standard TD3 backup
\begin{equation}
y
=
r^{\mathrm{ext}}+\gamma(1-d)\,Q_{\bar\phi}\bigl(s',u^{\mathrm{th}\prime},u^{\mathrm{bat}\prime},e_{m'},z'\bigr),
\label{eq:soft-v}
\end{equation}
where the target actor is deterministic in the mode,
\begin{equation}
m'=\arg\max_{m}\tilde\ell_{\bar\theta}(s'),
\label{eq:target-mode}
\end{equation}
clipped Gaussian noise is added \emph{only} to the magnitude,
\begin{equation}
\tilde z'=\mathrm{clip}\bigl(z'(s')+\varepsilon,0,1\bigr),
\qquad
\varepsilon\sim\mathrm{clip}\bigl(\mathcal{N}(0,\sigma),-c,c\bigr),
\label{eq:target-mag}
\end{equation}
and $\tilde z'$ is re-decoded with the \emph{next-state} intervals $\mathcal{M}_{m'}(s')$. Noise is not added to the decoded $u_{\mathrm{caes}}$. The twin critics minimise
\begin{equation}
L_Q
=
\mathbb{E}_{\mathcal{D}_{\mathrm{B}}}
\bigl[
(Q_{\phi_1}-y)^2+(Q_{\phi_2}-y)^2
\bigr].
\label{eq:lq}
\end{equation}
The actor (delayed, policy delay $D=2$) maximises $Q_{\phi_1}$ on the currently decoded hybrid action
\begin{equation}
L_{\pi}
=
\mathbb{E}_{s\sim\mathcal{D}_{\mathrm{B}}}
\bigl[-Q_{\phi_1}(s,u^{\mathrm{th}},u^{\mathrm{bat}},e_m,z)\bigr].
\label{eq:lpi}
\end{equation}
Only physical FMU transitions in $\mathcal{D}_{\mathrm{B}}$ enter~\eqref{eq:lq}--\eqref{eq:lpi}.

\subsection{Network architecture}
\label{sec:network}

The actor encoder is a two-layer MLP ($256$--ReLU--$256$--ReLU) on the $166$-dimensional observation. Linear heads emit thermal and battery commands, three mode logits and one magnitude logit. Twin critics share the same MLP width on the concatenated packing $(s,u^{\mathrm{th}},u^{\mathrm{bat}},e_m,z)$ (action dimension $6$). All Adam learning rates are $1\times10^{-4}$; gradient norms are clipped at $10$. Target networks track with $\tau=5\times10^{-3}$. The projection ablation uses a three-dimensional critic on $(u^{\mathrm{th}},u^{\mathrm{bat}},u_{\mathrm{caes}})$.

\subsection{Physical-only economic replay}
\label{sec:givesafe}

During stochastic training the controller may try up to $N_{\mathrm{try}}=64$ samples from $\pi_{\theta}$. Greedy evaluation tries the actor \emph{once}: repeating $a^\star$ does not enlarge $\mathcal{A}_f(s)$. A candidate is accepted only if it lies in $\mathcal{F}(s)$. Accepted physical transitions $(s,a,r^{\mathrm{ext}},s')$ enter the Bellman buffer $\mathcal{D}_{\mathrm{B}}$. Rejected proposals never advance the twin and never enter~\eqref{eq:lq}; they are written to a safety audit set $\mathcal{D}_{\mathrm{S}}$. Deterministic failure is recorded as \texttt{failed\_no\_safe\_action} with the first Oracle/Shadow reason; the training process still writes \texttt{summary.json}. GiveSafe is a residual screen on the joint decoder, not a rejection-sampling solver. We do not claim a new shield~\cite{garcia2015safe,sun2024constrained}.

\subsection{Ablations and classical baselines}
\label{sec:ablation}

Two learning controls isolate the support design; no further algorithm ablations are reported. (i)~\textbf{Continuous-projection TD3:} a single CAES latent is mapped by
\begin{equation}
u_{\mathrm{caes}}
=
\Pi_{\mathrm{bands}}\bigl(\tanh(z)\bigr),
\label{eq:project}
\end{equation}
where $\Pi_{\mathrm{bands}}$ clamps into the nearest charge/discharge band or to idle~$0$ when $\tanh(z)$ falls in the open gap. (ii)~\textbf{Static-support hybrid TD3:} mode masking as in~\eqref{eq:mask} but magnitude decoded onto the static device envelope~\eqref{eq:caes-legal}, with clamp only at execution. The four main methods compared in Section~\ref{sec:results} are a price-aware rule, a rolling MILP (binary CAES commitment on a linear energy surrogate, evaluated on the same FMU), continuous-projection TD3, and PC-HybridTD3. Modelling omissions of the MILP surrogate are stated in Section~\ref{sec:setup}.

\begin{algorithm}[!htbp]
\caption{PC-HybridTD3: physics-constrained parameterized hybrid TD3 on the DAE twin}
\label{alg:pc-hybridtd3}
\begin{algorithmic}[1]
\Require Actor $\pi_{\theta}$, twin critics, economic buffer $\mathcal{D}_{\mathrm{B}}$, safety set $\mathcal{D}_{\mathrm{S}}$, GiveSafe with fallback off
\State Collect $N_{\mathrm{warm}}=1024$ random-feasible physical hours on $\mathcal{A}_f(s)$; stop if $N_D\le 100$ or $N_C\le 100$
\For{episode $=1,2,\ldots$ until the physical-step budget}
  \State Reset Sysplorer FMU on a \emph{training}-week start; $s\leftarrow s_0$
  \For{$t=0$ to $T-1$}
    \State Device boxes $\leftarrow$ oracle; CAES $\cap$ grid window; mask empty modes
    \State Decode $u^{\mathrm{CAES}}$, tighten thermal, then battery (joint $\mathcal{A}_f(s)$)
    \State $a^{\star}\leftarrow$ GiveSafe($s,a$; $N_{\mathrm{try}}=64$ if stochastic, $1$ if greedy)
    \If{no legal $a^{\star}$}
      \State write first Oracle/Shadow reason to $\mathcal{D}_{\mathrm{S}}$; train: \textbf{break}; eval: \texttt{failed\_no\_safe\_action}
    \EndIf
    \State Step FMU one hour with $a^{\star}$; observe $s'$, $r^{\mathrm{ext}}$
    \State store $(s,a^{\star},r^{\mathrm{ext}},s',m,z,\mathcal{M}(s),\mathcal{M}(s'))$ in $\mathcal{D}_{\mathrm{B}}$
    \State Target: $m'\leftarrow\arg\max$; noise on $z'$ only; re-decode with $\mathcal{M}(s')$
    \State Update critics~\eqref{eq:lq}; delayed actor~\eqref{eq:lpi} with Straight-Through Gumbel
    \State $s\leftarrow s'$
  \EndFor
\EndFor
\Ensure Learned physics-constrained policy $\pi_{\theta}$
\end{algorithmic}
\end{algorithm}

\begin{table}[!htbp]
\centering
\caption{Main PC-HybridTD3 hyperparameters (fair stack).}
\label{tab:hyper}
\scriptsize
\begin{tabular}{@{}llll@{}}
\toprule
Symbol / name & Value & Symbol / name & Value \\
\midrule
$\gamma$ & 0.99 & target $\tau$ & $5\times10^{-3}$ \\
actor / critic lr & $1\times10^{-4}$ & batch size & 64 \\
Bellman buffer & $1\times10^{5}$ & GiveSafe tries (train / greedy) & 64 / 1 \\
policy delay $D$ & 2 & target mag.\ noise / clip & $0.2$ / $0.5$ \\
Gumbel $\tau_{\mathrm{G}}$ & $1.0\to 0.2$ & algorithm tag & \texttt{pc\_hybrid\_td3} \\
MLP hidden / act. & $256$ / ReLU & $Q$ clip / grad clip & $200$ / $10$ \\
warm start / $\varepsilon_{\mathrm{rand}}$ & $1024$ / $1.0\to 0.05$ & shadow FMU (seasonal) & off \\
$C_{\mathrm{ref}}$ & $1.565\times10^{5}$ CNY & terminal $\xi$ & 0.06 \\
terminal $B$ / fail pen. & 15 / 0 & episode length $T$ & 168 h \\
reported seeds & $0,1,2$ & physical budget / seed & $3$--$5\times10^{5}$ \\
observation dim & 166 & hybrid act.\ $(u^{\mathrm{th}},u^{\mathrm{bat}},m,z)$ & on $\mathcal{A}_f(s)$ \\
storage-use $R^F$ & off & forecast horizon & 24~h \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

% =====================================================================
\section{Simulation results}
\label{sec:results}
% =====================================================================

\subsection{Simulation settings}
\label{sec:setup}

The numerical study is a fair \emph{weekly} protocol on the same Sysplorer FMU, the monthly 110~kV TOU of Table~\ref{tab:tou-monthly}, and identical resource files. Each episode lasts \(T=168\)~h. The 8760~h calendar only supplies rotating week-start times; energy inventories are re-initialised at each reset. A continuous-year 8760~h rollout is labelled \emph{deployment evaluation} and is not mixed with held-out test scores. The 52 complete weeks are frozen and stratified by quarter, $9$~train /$2$~validation /$2$~test per season ($36/8/8$). Training episode starts are drawn only from the training IDs; checkpoint selection uses validation; Tables~\ref{tab:run}--\ref{tab:econ} report TEST weeks only. Weather, TOU windows (Table~\ref{tab:tou-windows}) and monthly delivered levels (Table~\ref{tab:tou-monthly}) therefore all change across seasons. Learning agents use seeds $0,1,2$ and a shared physical budget of $3$--$5\times10^{5}$ FMU hours (extended uniformly to $8.4\times10^{5}$ only if the shorter budget is still climbing). Runs that consume Table~\ref{tab:tou-monthly} are stored under a dedicated monthly-tariff tree and are not pooled with earlier year-constant TOU archives or with archived FS-HSAC directories. Hyperparameters are Table~\ref{tab:hyper}. GiveSafe fallback and shadow-FMU validation are off on this stack. The main matrix uses a perfect 24~h forecast; a noisy robustness case ($\sigma_{\mathrm{wind}}=10\%$, $\sigma_{\mathrm{solar}}=10\%$, $\sigma_{\mathrm{load}}=8\%$) is reported separately.

\paragraph{Methods.}
The four main methods are a \textbf{price-aware rule}, \textbf{rolling MILP} (optimise a linear energy surrogate, evaluate on the same FMU), \textbf{continuous-projection TD3}, and \textbf{PC-HybridTD3}. The only algorithm ablations are continuous versus hybrid heads, and static versus dynamic $\mathcal{A}_f(s)$. GiveSafe stays on for every learning agent; fallback is off. Held-out evaluation is deterministic. Empty cells in this draft are reserved for the monthly-tariff PC-HybridTD3 campaign; they are not filled from year-constant TOU archives or from FS-HSAC run trees.

\paragraph{Metrics.}
Primary ranking uses the full-week comprehensive monetary objective~\eqref{eq:J}: $J^{\mathrm{gen}}$ and $CC=-J^{\mathrm{gen}}$ are reported only when $\texttt{valid\_steps}=168$. Cost-breakdown columns expose cash, carbon, $C^{CUT}$, degradation, CAES startup and soft grid-contract charges. Independent physical KPIs are reported in parallel and never replace $CC$ as the economic ranking key:
\begin{itemize}
  \item \emph{Renewable uptake:} curtailment MWh, rate $E^{\mathrm{curt}}/E^{\mathrm{RES,avail}}$, utilisation.
  \item \emph{Flexibility / grid friendliness:} contract excess MWh and violation hours, $|P^{\mathrm{grid}}|_{\max}$, peak--valley and ramp of interchange (1~h resolution; no claim of sub-hourly response).
  \item \emph{Reliability / executability:} unserved MWh, valid steps, FMU failures, GiveSafe rejects, and the terminal inventory residual $E_{\mathrm{terminal}}=|s_{T,\mathrm{bat}}-s_{0,\mathrm{bat}}|+|s_{T,\mathrm{gas}}-s_{0,\mathrm{gas}}|$ (not only the binary bonus).
  \item \emph{Online compute:} mean/p95/max decision time; for rolling MILP/linprog also timeout and fail counts.
\end{itemize}
Hours survived and \texttt{eval\_failed} remain a separate executability table (Table~\ref{tab:run}); truncated sums are never compared with full weeks. Battery and gas SoC trajectories are qualitative; the week-end indicator~\eqref{eq:term} is a soft bonus, not a ranking KPI. Declaration rule: lowest $CC$ is economically best; better curtailment or fewer contract excesses is stated only on that axis; ``overall better'' is reserved for weak Pareto dominance on $CC$, curtailment and reliability together.

\subsection{Executability under the hard filter}

Table~\ref{tab:run} is the executability ledger on the monthly-tariff held-out weeks (GiveSafe fallback off). Hours are valid FMU steps; \texttt{failed\_no\_safe\_action} means the greedy actor was rejected once (not 64 identical retries). Year-constant TOU archives are a different price object and are not mixed with Table~\ref{tab:tou-monthly}.

\begin{table}[!htbp]
\centering
\caption{Executability on TEST weeks (seeds~$0$--$2$, monthly 110~kV TOU, GiveSafe fallback off). Hours are valid FMU steps. Empty cells: PC-HybridTD3 campaign in progress.}
\label{tab:run}
\scriptsize
\begin{tabular}{lllr}
\toprule
Season & Method & Status & Hours \\
\midrule
Winter / transition / summer / autumn & PC-HybridTD3 & --- & --- \\
Winter / transition / summer / autumn & projection TD3 & --- & --- \\
Winter / transition / summer / autumn & static-support hybrid TD3 & --- & --- \\
Winter / transition / summer / autumn & price-aware rule & --- & --- \\
Winter / transition / summer / autumn & rolling MILP & --- & --- \\
\bottomrule
\end{tabular}\\[2pt]
{\footnotesize \texttt{failed\_no\_safe\_action}: greedy actor rejected on the first GiveSafe check. Hours $<168$: evaluation stopped; partial trajectory is kept.}
\end{table}
\FloatBarrier

\subsection{Same-horizon economics}

\(R\) and \(J^{\mathrm{gen}}\) accumulate by the hour. Ranking a truncated sum against a 168~h sum would treat survival time as cash. Table~\ref{tab:econ} therefore keeps only \(\texttt{valid\_steps}=168\) and ranks by comprehensive cost $CC=-J^{\mathrm{gen}}$ (equivalently $J^{\mathrm{gen}}$). Cost-component columns (cash, carbon, $C^{CUT}$, wear, CAES start-up, soft contract) use the same full-week rows. Incomplete weeks do not enter Table~\ref{tab:econ}.

\begin{table}[!htbp]
\centering
\caption{Full-week TEST economics (seeds~$0$--$2$, monthly 110~kV TOU). Only \(\texttt{valid\_steps}=168\). Primary column $J^{\mathrm{gen}}$ ($10^{6}$~CNY) is~\eqref{eq:J}; $E^{\mathrm{th}}$ and battery throughput in MWh. Report $E_{\mathrm{terminal}}$ alongside the soft bonus. Empty cells: PC-HybridTD3 campaign in progress.}
\label{tab:econ}
\scriptsize
\begin{tabular}{llrrrr}
\toprule
Season & Method & $R$ & $J^{\mathrm{gen}}$ & $E^{\mathrm{th}}$ & Bat.\ thr. \\
\midrule
Winter & PC-HybridTD3 & --- & --- & --- & --- \\
Winter & projection TD3 & --- & --- & --- & --- \\
Winter & rolling MILP & --- & --- & --- & --- \\
Winter & price-aware rule & --- & --- & --- & --- \\
Transition / summer / autumn & PC-HybridTD3 & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

\subsection{Renewable uptake, flexibility and reliability}
\label{sec:kpi-physics}

Companion tables report curtailment MWh and rate, contract excess and violation hours, interchange peak--valley and ramp, unserved energy, and decision-time statistics for the same full-week rows as Table~\ref{tab:econ}. These axes do not override $CC$: a method with lower curtailment but higher $CC$ is a Pareto trade-off induced by prices and constraints, not a smarter solver. Mechanism ablation (full thermal--BESS--CAES versus lock-CAES under identical forecast and~\eqref{eq:J}) attributes $\Delta CC$, $\Delta E^{\mathrm{curt}}$ and interchange changes to device complementarity rather than to a single synthetic synergy index.

\subsection{Sensitivity and Pareto language}
\label{sec:sensitivity-rules}

Sensitivity axes include carbon price, feasibility-margin scale, CAES capacity, curtailment price $\nu_{\mathrm{curt}}$ and contract price $\nu_{\mathrm{grid}}$. If MILP records lower $CC$ but higher curtailment, the text reports the price-driven trade-off and checks it on the curtailment and contract sweeps; that gap is not an RL forecast advantage. The main matrix shares perfect forecast across methods.

\subsection{Scheduling traces and surrogate gap}

Closed-loop power-balance and inventory plots show only evaluations that completed \(T=168\)~h on Table~\ref{tab:tou-monthly}. Truncated rollouts and \texttt{eval\_failed} stay in Table~\ref{tab:run}. Training curves mix exploration noise and rotating weeks; they are not held-out scores.

\paragraph{Classical controls.}
The price-aware rule is an engineering heuristic on the same FMU. Rolling MILP uses a $H=24$~h receding horizon on a linear energy surrogate (binary CAES commitment and min-load bands), applies only the first hour $a_t^\star$, then the FMU steps to $s_{t+1}$ and the programme is resolved. Every method is therefore ranked on the \emph{same} Sysplorer twin, not ``RL on the FMU versus MILP on a linear model''. Incomplete weeks are not used to extend claims. The baselines diagnose the plant object; they are not global bounds on the FMU.

\subsection{KPI ledger}
\label{sec:kpi-ledger}

Reward is a training signal, not the ranking column. Table~\ref{tab:kpi} is the formal ledger; empty cells wait for Stage~D TEST weeks. Deterministic TEST GiveSafe rejection should be $<2\%$ (ideally $0$); hard violations and FMU failures should be $0$.

\begin{table}[!htbp]
\centering
\caption{Formal TEST-week KPI ledger (empty until Stage~D). Primary economic rank is comprehensive cost $CC=-J^{\mathrm{gen}}$.}
\label{tab:kpi}
\scriptsize
\begin{tabular}{llrrrr}
\toprule
Indicator & Unit & Rule & MILP & Proj.\ TD3 & PC-HybridTD3 \\
\midrule
Generalized operating cost $CC$ & CNY & --- & --- & --- & --- \\
Grid trading cost & CNY & --- & --- & --- & --- \\
Carbon cost & CNY & --- & --- & --- & --- \\
Curtailment & MWh & --- & --- & --- & --- \\
Curtailment rate & \% & --- & --- & --- & --- \\
Unserved energy & MWh & --- & --- & --- & --- \\
Battery throughput & MWh & --- & --- & --- & --- \\
CAES throughput & MWh & --- & --- & --- & --- \\
CAES charge / discharge hours & h & --- & --- & --- & --- \\
CAES mode switches & count & --- & --- & --- & --- \\
CAES gas SoC range & -- & --- & --- & --- & --- \\
Terminal SoC error $E_{\mathrm{terminal}}$ & -- & --- & --- & --- & --- \\
GiveSafe rejection rate & \% & --- & --- & --- & --- \\
Hard violations / FMU failures & count & --- & --- & --- & --- \\
Decision time & ms/step & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

\subsection{Typical TEST-week trace}
\label{sec:typical-week}

The headline figure is not a reward curve. A TEST-week dispatch plot is reserved until Stage~D trajectories exist: $P_{\mathrm{load}},P_{\mathrm{wind}},P_{\mathrm{PV}},P_{\mathrm{thermal}},P_{\mathrm{battery}},P_{\mathrm{CAES}},P_{\mathrm{grid}}$ in the upper band, $SOC_{\mathrm{bat/gas/hot/cold}}$ and CAES mode $D/I/C$ in the lower band, together with the TOU price. The visual claim is: low price / surplus renewables $\to$ CAES charge $\to$ gas SoC up; high price / high net load $\to$ CAES discharge $\to$ gas SoC down. If that story is not visible, the paper is not finished.

\subsection{Noisy forecast and 8760~h deployment}
\label{sec:deployment}

The main matrix uses a perfect 24~h forecast. A robustness case contaminates the observation only ($\sigma_{\mathrm{wind}}=10\%$, $\sigma_{\mathrm{solar}}=10\%$, $\sigma_{\mathrm{load}}=8\%$; temperature unperturbed) via \texttt{--forecast-mode noisy}. After TEST-week selection, the best policy is rolled for 8760~h \emph{without retraining}. That run is labelled a \textbf{full-year deployment study}: report $C_{\mathrm{annual}}$, $E^{\mathrm{cut}}_{\mathrm{annual}}$, CAES throughput, mode switches, hard violations and FMU failures. It is not a held-out test score and does not replace Table~\ref{tab:kpi}.

\subsection{Discussion}
\label{sec:support}

Tables~\ref{tab:econ}--\ref{tab:run} are the monthly 110~kV TEST ledger (obs~$=166$). Economics are same-horizon only. Executability is a separate axis from cash. Numbers will be entered from the monthly-tariff PC-HybridTD3 campaign once each method completes 168~h on TEST weeks. Continuous-year cold-tank viability and intensity carbon books stay in Appendix~\ref{app:annual-carbon}; they do not support an ``8760~h safe and best economics'' claim for RL.

Present limitations: results pending Stage~D; dynamic magnitude intervals mix twin physics with oracle margins; weekly-reset protocol for the main text; GiveSafe is adopted, not proposed. September--December buy levels in Table~\ref{tab:tou-monthly} are carry-forwards (S-grade) until official monthly tables appear. If CAES remains idle after the support-consistent actor, the diagnosis is the plant and the tariff, not a missing utilisation bonus $R^F$.

% =====================================================================
\section{Conclusion}
\label{sec:conclusion}
% =====================================================================

Weekly thermal--battery--CAES dispatch under monthly Shandong agency-purchase TOU and a National ETS carbon price is a comprehensive monetary problem whose hybrid actions live on a state-dependent support $\mathcal{A}_f(s)$ induced by a DAE plant twin. The main conclusions, without ranking cash on incomplete weeks, are as follows.
\begin{enumerate}
\item The plant is a first-principles Sysplorer Modelica object (thermal ramps, battery SoC, multi-tank CAES gas--thermal DAEs) exported as an FMI FMU for one-hour closed-loop interaction. FMI is the exchange standard, not a methodological novelty; algebraic energy-balance environments hide the support the twin actually accepts.
\item That object induces a hybrid support $\mathcal{A}_f(s)$ with admissible modes $\mathcal{K}(s)$ and mode-conditioned intervals $\mathcal{M}_k(s)$. The legal CAES envelope~\eqref{eq:caes-legal} and mode locks are operating constraints, not a claimed discovery.
\item PC-HybridTD3 is a same-timescale parameterized hybrid TD3 on that support: the actor decodes $(m,z)$ onto $\mathcal{A}_f(s)$, the critic scores $(e_m,z)$, the target keeps $\arg\max m'$ and noises only $z'$, and rejected GiveSafe hours never enter the economic replay $\mathcal{D}_{\mathrm{B}}$. Conventional continuous DRL cannot express CAES's disconnected modes; this work keeps a single-layer receding policy and embeds the FMU feasible set in the actor. It is not a $c$-step inventory hierarchy.
\end{enumerate}
Hierarchical RL remains applicable to inventory-timescale plants, and RL need not dominate MILP on cash. The one-sentence claim is: feasible hybrid action learning for CAES topology, not a more complex hierarchy than Cui et al. Limitations of the present draft are pending Stage~D numbers, oracle margins inside $\mathcal{M}_k(s)$, a weekly-reset main protocol, and September--December buy levels that remain carry-forwards until official monthly tables appear.

\section*{Acknowledgments}

Mengxi system-level boundaries and real-time nodal prices used in related cross-domain studies were provided through the AI4E track of the World Scientific Intelligence Competition; numerical weather prediction fields were supplied by TJ Weather. The authors gratefully acknowledge the organisers and data providers.

\section*{Data availability}

Plant model parameters, TOU reconstruction data and evaluation protocols are available from the authors upon reasonable request, subject to competition data-access rules where applicable.

% =====================================================================
\appendix
\section{Continuous-year viability and intensity carbon accounting}
\label{app:annual-carbon}
% =====================================================================

This appendix records implementation protocols that must \emph{not} be read as ``RL is 8760~h safe and economically optimal.''

\paragraph{Disconnected CAES set.}
Figure~\ref{fig:caes-set} recalls a schematic continuous projection onto the charge / idle / discharge bands (the main-text legal set is Fig.~\ref{fig:caes-legal}).

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.88\textwidth]{fig_caes_feasible_set}
\caption{Disconnected legal set of the continuous CAES command and a schematic projection from an illegal raw proposal onto a feasible band (ablation view).}
\label{fig:caes-set}
\end{figure}

\paragraph{Cold-tank transition guard.}
A sign error in the cold-tank mass balance of the one-step feasibility oracle caused continuous annual rollouts to assert-fail near $1276$~h for learning agents. After flipping the cold-tank guard and re-fitting transition coefficients, a rule / B0-style policy can complete $8760$~h (Fig.~\ref{fig:cold}). That fact is a twin-viability check; it is not evidence that hybrid SAC or projected TD3 already delivers annual closed-loop optimality.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.72\textwidth]{fig_cold_tank_guard}
\caption{Continuous-year survival before and after correcting the cold-tank transition guard.}
\label{fig:cold}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{fig_continuous_year_soc}
\caption{Illustrative continuous-year SoC trajectories (battery, gas, hot and cold tanks). Live evaluation CSVs replace the schematic when continuous annual evals are archived under \texttt{runs/}.}
\label{fig:cont-soc}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.9\textwidth]{fig_annual_weekly_reset}
\caption{Weekly-reset annual evaluation (52+ windows) used as a protocol contrast to continuous SoC carry.}
\label{fig:annual-reset}
\end{figure}

\paragraph{Intensity-benchmark carbon.}
The Python economic layer can accumulate an intensity allowance $A_t=\beta E^{\mathrm{th}}_t$ and emissions $E_t=\eta_{\mathrm{th}}E^{\mathrm{th}}_t$, settle $Q=A-E$ at episode end, and expose normalised position, degradation fraction and year progress as three auxiliary observation channels (Figs.~\ref{fig:carbon-pos}--\ref{fig:aux}). Main seasonal tables quote the intensity-benchmark book with $\pi_{\mathrm{CO_2}}=97.49$~CNY/t unless a sensitivity row is marked otherwise.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.92\textwidth]{fig_carbon_position}
\caption{Intensity-benchmark carbon books: cumulative allowance $A$, emissions $E$ and position $Q=A-E$.}
\label{fig:carbon-pos}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{fig_carbon_settlement}
\caption{Illustrative year-end carbon settlement by method (replace with live continuous-year evals when available).}
\label{fig:carbon-settle}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.92\textwidth]{fig_aux_obs}
\caption{Auxiliary observation channels appended to the physical--forecast--market vector (observation dimension $166$).}
\label{fig:aux}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{fig_givesafe_reject}
\caption{Safety--learning coupling: reject rate versus held-out reward across learning agents (archive / schematic).}
\label{fig:reject}
\end{figure}

\bibliographystyle{elsarticle-num}
\bibliography{references}

\end{document}
