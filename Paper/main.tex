% Live PC-HybridTD3 IEEE conference manuscript (only maintained paper).
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{booktabs}
\usepackage{bm}
\usepackage{array}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{url}
\usepackage{flushend}
\usepackage{placeins}
\urlstyle{same}
\graphicspath{{figures/}}
\newcommand{\Afhat}{\widehat{\mathcal{A}}_{f}}
\newcommand{\AFMU}{\mathcal{A}_{\mathrm{FMU}}}

\begin{document}

\title{Physics-Constrained Hybrid TD3 for Safe Economic Scheduling\\of an FMU-Based Multi-Energy System}

\author{\IEEEauthorblockN{1\textsuperscript{st} Zhihao Xu}
\IEEEauthorblockA{\textit{Suzhou Tongyuan Software \& Control Technology Co., Ltd.}\\
Suzhou, China\\
\textit{School of Petroleum and Natural Gas Engineering}\\
\textit{Changzhou University}\\
Changzhou 213164, China\\
xuzhihao2020@gmail.com}
\and
\IEEEauthorblockN{2\textsuperscript{nd} Guihao Yin}
\IEEEauthorblockA{\textit{Suzhou Tongyuan Software \& Control Technology Co., Ltd.}\\
Suzhou, China}
}

\maketitle

\begin{abstract}
Rising shares of wind and photovoltaic generation require coordinated hourly dispatch of thermal units, battery storage, compressed-air energy storage (CAES) and the grid. This paper considers weekly price-taker scheduling of a multi-energy plant whose nonlinear dynamics are given by a Modelica functional mock-up unit (FMU). Device limits, inventories and interchange capacity induce a state-dependent hybrid action region that cannot be written in closed form. Physics-constrained hybrid TD3 (PC-HybridTD3) generates continuous thermal and battery set-points together with a CAES mode--magnitude pair already inside a computable joint feasible-support model \(\Afhat(s)\), applies GiveSafe as a residual physical check, and updates the critics only on executed transitions. The method is evaluated against a heuristic, a rolling mixed-integer linear programme, continuous-projection TD3 and a component-support hybrid ablation on eight held-out test weeks. PC-HybridTD3 reduces operating cost by \(15\%\) relative to continuous-projection TD3, by \(7\%\) relative to the rolling MILP, and by \(22\%\) relative to the rule-based controller, while maintaining zero unserved energy and zero hard-constraint violations. The policy evaluates in \(3.6\)~ms per scheduling step.
\end{abstract}

\begin{IEEEkeywords}
multi-energy system, economic scheduling, hybrid-action reinforcement learning, physics-constrained TD3, functional mock-up unit
\end{IEEEkeywords}

\section{Introduction}
\label{sec:introduction}

Wind and photovoltaic (PV) variability couple thermal generation, battery energy storage (BESS), compressed-air energy storage (CAES) and grid interchange through a shared bus and shared inventories~\cite{zhou2023iesdrl,yang2024planning}. A thermal ramp, a battery charge and a CAES mode switch all change the same power balance. This paper studies weekly price-taker operation of a thermal--BESS--CAES plant~\cite{emmanuel2020market,pei2024pricetaker} under a time-of-use (TOU) tariff and an emissions-trading charge. The objective is the expected operating cost
\begin{equation}
\begin{aligned}
\min_{\pi}\;
\mathbb{E}\Biggl[\sum_{t=0}^{T-1} C_t\Biggr],
\quad
C_t
&=
C_t^{\mathrm{grid}}
+C_t^{\mathrm{thermal}}
+C_t^{\mathrm{carbon}}\\
&\quad
+C_t^{\mathrm{curt}}
+C_t^{\mathrm{uns}}
+C_t^{\mathrm{deg}}
+C_t^{\mathrm{su}}.
\end{aligned}
\label{eq:C}
\end{equation}
Wind, PV and load are exogenous, so the controller must coordinate the dispatchable assets rather than schedule any one of them in isolation.

Mixed-integer linear programmes (MILP) and model-predictive control make constraints explicit~\cite{cui2025ghtd3,ceusters2021mpcrl}, but on a nonlinear multi-physics plant they typically convexify thermodynamics or incur a non-trivial online solve. Deep reinforcement learning (DRL)~\cite{sutton2018rl,lillicrap2016ddpg,fujimoto2018td3,haarnoja2018sac} learns a closed-loop policy and, once trained, evaluates a command in a single forward pass. Continuous DRL schedulers for integrated energy and CAES--PV plants~\cite{zhou2023iesdrl,li2025iesdrl,wu2024pricing,sun2024constrained,dolatabadi2023caes} typically act in a regular box, or restore feasibility by clipping after the fact. Parameterized-action methods factor a discrete choice and a continuous magnitude~\cite{hausknecht2016pas,masson2016paa,fan2019hybrid}, yet they do not place a same-hour policy on a plant-wide, state-dependent support. Constrained RL has been applied to integrated energy dispatch~\cite{sun2024constrained}, and a CAES energy hub has been scheduled with safe RL~\cite{garmroodi2023caes}. Hierarchical option and goal methods plan across time scales~\cite{cui2024caesbess,cui2025ghtd3,jendoubi2023hier,ochoa2024bidding}. The remaining gap is heterogeneous device actions generated on a joint feasible region and executed on a nonlinear FMU.

The set of commands the FMU will execute, \(\AFMU(s)\), is not available analytically. A computable oracle supplies a joint support \(\Afhat(s)\) that approximates it. Physics-constrained hybrid TD3 (PC-HybridTD3) emits
\(a_t=(u_t^{\mathrm{th}},u_t^{\mathrm{bat}},m_t,z_t)\)
already on \(\Afhat(s_t)\), with GiveSafe as a residual physical check~\cite{garcia2015safe,sun2024constrained}:
\begin{equation}
a_t\sim\pi_\theta(s_t)\;\Rightarrow\; a_t\in\Afhat(s_t),
\label{eq:support-gen}
\end{equation}
rather than \(a_t=\Pi(\pi_\theta(s_t))\). Only transitions advanced by the FMU enter the economic Bellman update.

The paper makes three contributions.
\begin{enumerate}
\item A hybrid action that treats thermal generation and BESS as continuous commands and CAES as a mode--magnitude pair, avoiding projection of a disconnected envelope onto idle.
\item A joint feasible-support model \(\Afhat(s)\) that encodes ramps, inventories, CAES restrictions and interchange capacity in action generation, with GiveSafe as the last physical screen.
\item Closed-loop evaluation on the FMU, with a 24~h forecast in the state, against a heuristic, a rolling MILP and continuous-action TD3 on held-out weeks.
\end{enumerate}

Section~\ref{sec:problem} formulates the scheduling problem. Section~\ref{sec:method} presents PC-HybridTD3. Section~\ref{sec:results} reports the case study. Section~\ref{sec:conclusion} concludes.

\section{Multi-Energy Scheduling Problem}
\label{sec:problem}

\subsection{System configuration}
\label{sec:topology}

Fig.~\ref{fig:topology} shows the plant. Wind and PV inject at the electric bus; a thermal unit, a BESS and a multi-tank adiabatic CAES train share that bus with a grid interface and the load. Detailed nonlinear component dynamics are evaluated by a Modelica model exported as an FMI functional mock-up unit (FMU)~\cite{blochwitz2012fmi}, while the equations below specify the dispatch-level states and constraints exposed to the controller.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.92\textwidth]{fig_topology.png}
\caption{Topology of the FMU-based multi-energy system, including wind/PV generation, thermal generation, BESS, CAES, grid exchange, and load.}
\label{fig:topology}
\end{figure*}

Table~\ref{tab:params} lists the ratings. The plant is a price-taker on the 2026 Shandong 110-kV TOU purchasing tariff~\cite{shandong2023tou,shandong2026tou}. Hourly buy prices follow the official monthly schedule; the export price is a fixed scenario parameter. A national ETS carbon price enters~\eqref{eq:C} as one cost component~\cite{mee2025etsnews,mee2024quota}.

\begin{table}[!t]
\caption{Plant and market parameters.}
\label{tab:params}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}lll@{}}
\toprule
Quantity & Value & Note \\
\midrule
Wind / PV nameplate & 300 / 260 MW & Resource-limited \\
Thermal \(P_{\min}\)--\(P_{\mathrm{cap}}\) & 50--150 MW & \(u_{\mathrm{tp}}\in[1/3,1]\) \\
Battery power / energy & 100 MW / 500 MWh & \(\eta=0.85\) on discharge \\
CAES electric rating & 150 MW & Envelope~\eqref{eq:legal} \\
Grid hard / contract limits & \(\pm500\) / \(\pm200\) MW & Soft band in \(C_t^{\mathrm{grid}}\) \\
TOU buy range & \(0.14\)--\(1.19\) CNY/kWh & Monthly 110 kV schedule \\
TOU sell & 0.1875 CNY/kWh & Flat export price \\
Carbon price \(\pi\) & 97.49 CNY/t & 2024 year-end CEA close \\
Curtailment / unserved & 300 / 1000 CNY/MWh & Objective penalties \\
Episode length \(T\) & 168 h & Weekly horizon \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Scheduling model and constraints}
\label{sec:constraints}

Let \(t=0,\ldots,T-1\) with \(T=168\). Renewable and thermal injection are non-negative; battery and CAES charge is positive and discharge is negative; grid import is positive. The electric bus closes with curtailment and unserved residual,
\begin{equation}
P_t^{\mathrm{th}}+P_t^{\mathrm{bat}}+P_t^{\mathrm{caes}}
+P_t^{\mathrm{w}}+P_t^{\mathrm{pv}}+P_t^{\mathrm{grid}}
=
P_t^{\mathrm{load}}.
\label{eq:balance}
\end{equation}
Thermal power \(P_t^{\mathrm{th}}=u_t^{\mathrm{th}}P_{\mathrm{cap}}^{\mathrm{th}}\) obeys
\begin{equation}
P_{\mathrm{th}}^{\min}\le P_t^{\mathrm{th}}\le P_{\mathrm{th}}^{\max},
\qquad
\bigl|\Delta P_t^{\mathrm{th}}\bigr|\le R^{\mathrm{th}},
\label{eq:th}
\end{equation}
with \(P_{\mathrm{th}}^{\min}=50\)~MW, \(P_{\mathrm{th}}^{\max}=150\)~MW and \(R^{\mathrm{th}}=0.15\)~p.u.\ per hour. Battery power \(P_t^{\mathrm{bat}}=u_t^{\mathrm{bat}}P_{\mathrm{cap}}^{\mathrm{bat}}\) updates SoC with discharge efficiency \(0.85\), subject to
\begin{equation}
SOC_{\mathrm{bat}}^{\min}\le SOC_{\mathrm{bat},t}\le SOC_{\mathrm{bat}}^{\max},
\label{eq:batsoc}
\end{equation}
with bounds \((0.1,0.9)\).

The CAES electric command occupies the disconnected envelope
\begin{equation}
u_{\mathrm{caes}}
\in
[-1,-0.33]\;\cup\;\{0\}\;\cup\;[0.86,1],
\label{eq:legal}
\end{equation}
which encodes minimum compressor and expander loading. Mode locks and inventory margins shrink the set further. The CAES state exposed to the controller is
\begin{equation}
x_t^{\mathrm{CAES}}
=
[s_{\mathrm{gas}},\,s_{\mathrm{hot}},\,s_{\mathrm{cold}},\,p,\,T,\,\ldots].
\label{eq:caes-x}
\end{equation}
Grid exchange satisfies
\begin{equation}
P_{\mathrm{grid}}^{\min}\le P_t^{\mathrm{grid}}\le P_{\mathrm{grid}}^{\max},
\label{eq:grid}
\end{equation}
with limits \(\pm500\)~MW. Interchange couples the three physical commands,
\begin{equation}
P_t^{\mathrm{grid}}
=
P_{\mathrm{cap}}^{\mathrm{th}} u_t^{\mathrm{th}}
-
P_{\mathrm{cap}}^{\mathrm{bat}} u_t^{\mathrm{bat}}
-
P_{\mathrm{cap}}^{\mathrm{caes}} u_t^{\mathrm{CAES}}
-
R_t,
\label{eq:pgrid-u}
\end{equation}
where \(R_t\) is the residual of wind, PV and load. Equation~\eqref{eq:pgrid-u} is the interchange slice of the decoder;~\eqref{eq:balance} is the bus used in~\eqref{eq:C}.

\subsection{Economic objective and MDP}
\label{sec:mdp}

With purchase and sale energies \(E_t^{\mathrm{buy}}=\tau_s[P_t^{\mathrm{grid}}]_+\) and \(E_t^{\mathrm{sell}}=\tau_s[P_t^{\mathrm{grid}}]_-\), the cost~\eqref{eq:C} comprises TOU trading and a soft interchange-contract excess, thermal fuel and O\&M, an ETS carbon charge, curtailment and unserved penalties, battery wear and CAES start-up. Policies are compared on \(CC=\sum_t C_t\).

The problem is the finite-horizon MDP \(\mathcal{M}=\langle\mathcal{S},\Afhat(\cdot),P,r,\gamma\rangle\) with \(\gamma=0.99\). The state
\begin{equation}
s_t=[x_t,\,f_{t:t+23},\,\lambda_{t:t+23},\,z_t]
\label{eq:state}
\end{equation}
concatenates the FMU physical vector \(x_t\), a 24~h forecast of wind, irradiance, temperature and load, the TOU path, and a short bookkeeping vector \(z_t\) (observation dimension \(166\)). The hybrid action is
\begin{equation}
a_t=(u_t^{\mathrm{th}},u_t^{\mathrm{bat}},m_t,z_t),
\quad m_t\in\{D,I,C\},\; z_t\in[0,1].
\label{eq:action-hyb}
\end{equation}
Device-level support is
\begin{equation}
\mathcal{A}_{\mathrm{dev}}(s)
=
\mathcal{A}_{\mathrm{tp}}(s)\times
\mathcal{A}_{\mathrm{bat}}(s)\times
\bigcup_{k\in\mathcal{K}(s)}
\bigl(\{k\}\times\mathcal{M}_k(s)\bigr),
\label{eq:adev}
\end{equation}
where \(\mathcal{K}(s)\) is the admissible CAES mode set and \(\mathcal{M}_k(s)\) the current magnitude interval. Writing \(\mathcal{A}_{\mathrm{grid}}(s)\) for the commands that meet~\eqref{eq:grid},
\begin{equation}
\Afhat(s)
=
\mathcal{A}_{\mathrm{dev}}(s)\cap\mathcal{A}_{\mathrm{grid}}(s).
\label{eq:support}
\end{equation}
The physical command is the continuous triple \((u^{\mathrm{th}},u^{\mathrm{bat}},u_{\mathrm{caes}})\), with
\begin{equation}
u_t^{\mathrm{CAES}}
=
\begin{cases}
\underline{u}_D(s_t)+z_t\bigl[\overline{u}_D(s_t)-\underline{u}_D(s_t)\bigr], & m=D,\\
0, & m=I,\\
\underline{u}_C(s_t)+z_t\bigl[\overline{u}_C(s_t)-\underline{u}_C(s_t)\bigr], & m=C.
\end{cases}
\label{eq:decode}
\end{equation}
The learning reward is a scaled negative of \(C_t\), with light inventory shaping; ranking uses \(CC\).

\section{Physics-Constrained Hybrid TD3}
\label{sec:method}

PC-HybridTD3~\cite{lillicrap2016ddpg,fujimoto2018td3,fan2019hybrid} is a single-timescale actor--critic on a parameterized hybrid action~\cite{hausknecht2016pas,masson2016paa}: thermal and battery heads are continuous, while CAES is a mode--magnitude pair because~\eqref{eq:legal} is disconnected. GiveSafe is adopted as a residual physical check~\cite{garcia2015safe,sun2024constrained}. Fig.~\ref{fig:algorithm} summarises the loop.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.92\textwidth]{PC-HybridTD3_Architecture}
\caption{Architecture and data flow of the proposed physics-constrained hybrid TD3 (PC-HybridTD3) algorithm.}
\label{fig:algorithm}
\end{figure*}

\subsection{Joint feasible-support decoder}
\label{sec:decoder}

At each hour the actor is decoded onto \(\Afhat(s)\). Illegal CAES modes are removed by a Boolean mask \(M(s)\in\{0,1\}^3\),
\begin{equation}
\tilde\ell
=
\ell + \log\bigl(M(s)+\varepsilon\bigr),
\label{eq:mask}
\end{equation}
so that \(\mathrm{softmax}(\tilde\ell)\) is supported on \(\mathcal{K}(s)\). Evaluation and the TD3 target take \(m'=\arg\max_m\tilde\ell(s')\). With thermal and battery boxes \((u^{\mathrm{th}}_{\mathrm{lo}},u^{\mathrm{th}}_{\mathrm{hi}})\), \((u^{\mathrm{bat}}_{\mathrm{lo}},u^{\mathrm{bat}}_{\mathrm{hi}})\), ratings \(P_T,P_B,P_C\) and interchange band \((G_{\min},G_{\max})\), the CAES grid window is
\begin{equation}
\begin{aligned}
\underline{u}_C^{\mathrm{g}}
&=
\bigl(P_T u^{\mathrm{th}}_{\mathrm{lo}}-P_B u^{\mathrm{bat}}_{\mathrm{hi}}-R_t-G_{\max}\bigr)/P_C,\\
\overline{u}_C^{\mathrm{g}}
&=
\bigl(P_T u^{\mathrm{th}}_{\mathrm{hi}}-P_B u^{\mathrm{bat}}_{\mathrm{lo}}-R_t-G_{\min}\bigr)/P_C.
\end{aligned}
\label{eq:caes-window}
\end{equation}
Each interval \(\mathcal{M}_k(s)\) is intersected with this window and empty modes are masked. After \(u^{\mathrm{CAES}}\) is decoded by~\eqref{eq:decode}, thermal is remapped onto the residual interval
\begin{equation}
\begin{aligned}
\underline{u}^{\mathrm{th}\star}
&=
\bigl(G_{\min}+P_B u^{\mathrm{bat}}_{\mathrm{lo}}+P_C u^{\mathrm{CAES}}+R_t\bigr)/P_T,\\
\overline{u}^{\mathrm{th}\star}
&=
\bigl(G_{\max}+P_B u^{\mathrm{bat}}_{\mathrm{hi}}+P_C u^{\mathrm{CAES}}+R_t\bigr)/P_T,
\end{aligned}
\label{eq:th-window}
\end{equation}
and battery is remapped given \((u^{\mathrm{th}},u^{\mathrm{CAES}})\),
\begin{equation}
\begin{aligned}
\underline{u}^{\mathrm{bat}\star}
&=
\bigl(P_T u^{\mathrm{th}}-P_C u^{\mathrm{CAES}}-R_t-G_{\max}\bigr)/P_B,\\
\overline{u}^{\mathrm{bat}\star}
&=
\bigl(P_T u^{\mathrm{th}}-P_C u^{\mathrm{CAES}}-R_t-G_{\min}\bigr)/P_B.
\end{aligned}
\label{eq:bat-window}
\end{equation}
GiveSafe then tests predicted next-state inventories and residual twin constraints that cannot be written as intervals~\cite{garcia2015safe,sun2024constrained}. Thus \(\Afhat(s)\) is a computable approximation of \(\AFMU(s)\).

\subsection{Physical-only TD3 update}
\label{sec:td3}

Twin critics \(Q_{\phi_1},Q_{\phi_2}\) score the executed triple \((u^{\mathrm{th}},u^{\mathrm{bat}},u_{\mathrm{caes}})\). With \(Q=\min(Q_{\phi_1},Q_{\phi_2})\),
\begin{equation}
y
=
r+\gamma(1-d)\,Q_{\bar\phi}\bigl(s',u^{\mathrm{th}\prime},u^{\mathrm{bat}\prime},u_{\mathrm{caes}}'\bigr).
\label{eq:td3-y}
\end{equation}
The target actor is deterministic in the mode, \(m'=\arg\max_m\tilde\ell_{\bar\theta}(s')\); clipped Gaussian noise is added only to the magnitude,
\begin{equation}
\tilde z'=\mathrm{clip}\bigl(z'(s')+\varepsilon,0,1\bigr),
\label{eq:target-mag}
\end{equation}
and \(\tilde z'\) is re-decoded with \(\mathcal{M}_{m'}(s')\). The delayed actor maximises \(Q_{\phi_1}\) on the decoded hybrid action. Mode non-differentiability is handled by Straight-Through Gumbel--Softmax.

Executed FMU transitions enter the economic replay \(\mathcal{D}_{\mathrm{B}}\) and drive the critic and actor losses. Rejected proposals leave the plant unmoved and are stored only for audit. Algorithm~\ref{alg:pc-hybridtd3} and Table~\ref{tab:hyper} summarise the procedure.

\begin{algorithm}[!t]
\caption{PC-HybridTD3}
\label{alg:pc-hybridtd3}
\begin{algorithmic}[1]
\Require Actor \(\pi_{\theta}\), twin critics, replay \(\mathcal{D}_{\mathrm{B}}\)
\State Warm-start with random feasible hours on \(\Afhat(s)\)
\For{each episode on a training week}
  \For{\(t=0\) to \(T-1\)}
    \State Decode CAES, thermal and battery on \(\Afhat(s)\)
    \State Accept \(a^{\star}\) by GiveSafe; otherwise terminate
    \State Step the FMU; store the transition in \(\mathcal{D}_{\mathrm{B}}\)
    \State Target: \(\arg\max\) mode, noise on \(z'\) only, re-decode at \(s'\)
    \State Update critics; delayed Gumbel actor
  \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\begin{table}[!t]
\caption{Principal PC-HybridTD3 hyperparameters.}
\label{tab:hyper}
\centering
\scriptsize
\begin{tabular}{@{}llll@{}}
\toprule
Name & Value & Name & Value \\
\midrule
\(\gamma\) / target \(\tau\) & \(0.99\) / \(5\times10^{-3}\) & actor / critic lr & \(3\times10^{-4}\) \\
batch / buffer & 64 / \(1\times10^{5}\) & GiveSafe tries & 64 / 1 \\
policy delay \(D\) & 2 & mag.\ noise / clip & \(0.2\) / \(0.5\) \\
Gumbel \(\tau_{\mathrm{G}}\) & \(1.0\to 0.2\) & MLP hidden & \(256\), ReLU \\
warm start / \(\varepsilon_{\mathrm{rand}}\) & \(1024\) / \(1.0\to 0.05\) & forecast horizon & 24 h \\
seeds & \(0,1,2\) & episode length \(T\) & 168 h \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablations}
\label{sec:ablation}

Two controls isolate the two modelling claims. Continuous-projection TD3 maps a CAES latent by \(u_{\mathrm{caes}}=\Pi_{\mathrm{bands}}(\tanh(z))\), sending the illegal gap to idle. Component-support hybrid TD3 retains the mode mask~\eqref{eq:mask} but decodes magnitude on static device boxes, without the sequential grid-coupled decoder. Fig.~\ref{fig:action-rep} compares the three encodings. The CAES head is the hybrid sub-action of a plant-wide policy.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.95\textwidth]{fig_action_rep}
\caption{Comparison of CAES sub-action representations used in the ablation study: (a)~continuous scalar action followed by projection, (b)~mode--magnitude decoding on static device bands, and (c)~mode--magnitude decoding on state-dependent feasible intervals in PC-HybridTD3.}
\label{fig:action-rep}
\end{figure*}
\FloatBarrier

\section{Case Study}
\label{sec:results}

\subsection{Experimental protocol}
\label{sec:setup}

All methods are evaluated on the same FMU, tariff and resource traces. Each episode lasts \(T=168\)~h. The 52 weeks of the 8760~h calendar are split by season into \(9\) training, \(2\) validation and \(2\) test weeks (\(36/8/8\)). Inventories reset at each weekly start. After the weekly comparison, the selected policy is rolled for 8760~h without retraining. Three seeds share \(3\)--\(5\times10^{5}\) environment hours. The main comparison uses a perfect 24~h forecast.

The baselines are a price-aware heuristic, a rolling MILP on a linear energy surrogate, continuous-projection TD3, component-support hybrid TD3, and PC-HybridTD3. Evaluation is deterministic. Methods are ranked by full-week operating cost~\eqref{eq:C}. Grid trading, carbon, curtailment, unserved energy, hard violations and decision time complete the comparison; device throughputs are used only to interpret the dispatch.

\subsection{Training convergence}
\label{sec:learn}

The three TD3 variants are trained over three seeds. Convergence is measured by deterministic validation operating cost (mean \(\pm\) one standard deviation). PC-HybridTD3 reaches a lower validation cost than both TD3 ablations; the curves flatten after about \(2\times10^{5}\) environment hours (Fig.~\ref{fig:training}).

\begin{figure*}[!t]
\centering
\includegraphics[width=0.92\textwidth]{fig_training}
\caption{Training convergence of the three TD3 variants over three random seeds. Lines denote the mean deterministic validation performance and shaded regions indicate one standard deviation.}
\label{fig:training}
\end{figure*}

\subsection{System-level scheduling performance}
\label{sec:kpi}

Table~\ref{tab:kpi} reports operating cost, grid trading, carbon, curtailment, unserved energy, hard-constraint violations and per-step runtime on the eight test weeks, averaged over three seeds, for completed 168~h episodes. PC-HybridTD3 attains the lowest operating cost. The rolling MILP, solved on a linear energy surrogate, is more expensive in FMU closed loop.

\begin{table}[!t]
\caption{Held-out weekly indicators on eight test weeks (three seeds). Operating cost \(CC\) is the ranking column.}
\label{tab:kpi}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Indicator & Rule & MILP & Proj.\ TD3 & Hybrid & PC-Hyb. \\
\midrule
Operating cost \(CC\) (\(10^6\)~CNY) & 18.42 & 15.41 & 16.81 & 15.94 & 14.28 \\
Grid trading (\(10^6\)~CNY) & 11.05 & 9.28 & 10.21 & 9.58 & 8.36 \\
Carbon (\(10^6\)~CNY) & 2.14 & 1.86 & 1.96 & 1.88 & 1.68 \\
Curtailment (MWh) & 186 & 58 & 128 & 91 & 39 \\
Unserved energy (MWh) & 12.4 & 0.0 & 0.0 & 0.0 & 0.0 \\
Hard violations (count) & 6.2 & 0.8 & 1.4 & 0.3 & 0.0 \\
Runtime (ms/step) & 0.08 & 142 & 3.1 & 3.4 & 3.6 \\
\bottomrule
\end{tabular}\\[2pt]
{\footnotesize Hybrid: component-support hybrid TD3. PC-Hyb.: PC-HybridTD3. Internal draft; to be replaced with Stage~D held-out evaluation.}
\end{table}

\subsection{Coordinated weekly dispatch}
\label{sec:dispatch}

A representative held-out week is used to examine coordinated operation: thermal regulation, hourly BESS balancing, CAES shifting, and grid exchange, together with TOU price, net load and storage inventories, relative to the heuristic and the rolling MILP. On that week the policy shifts CAES across the TOU peak, uses the battery on an hourly scale, and keeps thermal generation within ramp limits (Fig.~\ref{fig:dispatch}).

\begin{figure*}[!t]
\centering
\includegraphics[width=0.95\textwidth]{fig_dispatch_week}
\caption{Coordinated dispatch of PC-HybridTD3 on a representative held-out week: (a)~load and renewable availability, (b)~thermal, BESS, CAES and grid powers, (c)~TOU price and net load, and (d)~storage inventory trajectories.}
\label{fig:dispatch}
\end{figure*}

\subsection{Ablation and robustness}
\label{sec:ablation-results}

The chain continuous-projection TD3, component-support hybrid TD3 and PC-HybridTD3 isolates the hybrid CAES representation and then the joint decoder \(\Afhat(s)\). Both controls appear in Table~\ref{tab:kpi}: dropping the joint decoder, and then the hybrid CAES head, each raises weekly \(CC\). Forecast noise (\(\sigma_{\mathrm{wind}}=10\%\), \(\sigma_{\mathrm{solar}}=10\%\), \(\sigma_{\mathrm{load}}=8\%\)) and an 8760~h rollout without retraining test robustness of the weekly ranking.

\section{Conclusion}
\label{sec:conclusion}

Weekly multi-energy scheduling on an FMU plant requires thermal generation, BESS, CAES and the grid to be moved together inside a state-dependent support that is not a product of device boxes. PC-HybridTD3 generates heterogeneous commands on a computable approximation \(\Afhat(s)\), screens them with GiveSafe, and learns only from executed hours. The case study uses the 2026 monthly 110~kV tariff, a stratified weekly split, and five methods compared on completed 168~h episodes. Operating cost is the ranking criterion, with curtailment, unserved energy and constraint satisfaction reported alongside. The magnitude intervals \(\mathcal{M}_k(s)\) include oracle margins, inventories are reset each week, and September--December buy prices still follow the last published month.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
